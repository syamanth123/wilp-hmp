# HMP — Dev Handoff Audit

Joining-engineer audit of the HMP codebase. Read this before touching code.
Source files cited inline; everything here is grounded in actual code, not the README.

---

## 1. Conventions cheat-sheet

### How a server action is structured

Every mutating server action follows the same recipe. Cross-reference:
[ic/requests/new/actions.ts](../apps/web/src/app/ic/requests/new/actions.ts),
[hog/requests/[id]/actions.ts](../apps/web/src/app/hog/requests/[id]/actions.ts),
[pc/requests/[id]/actions.ts](../apps/web/src/app/pc/requests/[id]/actions.ts),
[faculty/assignments/[id]/actions.ts](../apps/web/src/app/faculty/assignments/[id]/actions.ts).

1. `'use server';` directive at top of file.
2. `const actor = requireRole(await getSessionUser(), RoleName.X)` — auth + role gate.
3. Zod `.safeParse(...)` on `formData.get('...')` fields. On failure return `{ error: parsed.error.issues[0]?.message ?? 'Invalid input' }`.
4. Pre-flight DB load + business-rule check (status equality, ownership of assignment).
5. `try { await transition({ requestId, event, actor, meta?, effects? }); } catch (err) { if (err instanceof WorkflowError) return { error: err.message }; throw err; }`.
6. After transition, fire `notifyTransition({ requestId, event, actor: { id, name } })` — wrapped in try/catch internally; never blocks.
7. `revalidatePath` for _every_ role view of that request (IC, HOG, PC, Faculty, list pages, dashboard).
8. Return `{ ok: true }` on success. Use `redirect(...)` only for creation flows (e.g. `createRequestAction`).

### How transitions are called

`transition()` from `@hmp/workflow` ([packages/workflow/src/transition.ts](../packages/workflow/src/transition.ts)) is the **only** sanctioned way to change `HandoutRequest.status`. It:

- Calls `assertRoleAllowed(event, actor.roles)` against `EVENT_ROLE_MATRIX` ([guards.ts:8](../packages/workflow/src/guards.ts)). `ADMIN` bypasses.
- Looks up `nextStatus()` from the transition table in [machine.ts:53](../packages/workflow/src/machine.ts). Throws `WorkflowError('invalid_transition', ...)` on miss.
- Opens a `prisma.$transaction` that:
  1. updates `HandoutRequest.status`,
  2. lazily creates the `Handout` shell on first transition out of DRAFT (otherwise mirrors status onto `Handout`),
  3. runs the caller-supplied `effects(tx, ctx)` (e.g. write `FacultyAssignment`, `Approval`, `LmsPublishLog`, append `HandoutVersion`),
  4. writes one `AuditLog` row with `action = 'handout.transition.${event}'`, `before = { status: from }`, `after = { status: to, meta }`.
- If `effects` throws inside the txn, the status change is rolled back. This is the correct place to put concurrency guards (e.g. off-campus cap re-check inside `allocateFacultyAction`).

### How notifications are fired

[apps/web/src/lib/notifications.ts](../apps/web/src/lib/notifications.ts) exposes three entry points:

- `notifyTransition({ requestId, event, actor })` — workflow side-channel. Loads template via `EVENT_TEMPLATE_KEY[event]`, falls back to `INLINE_FALLBACK` if the `NotificationTemplate` row is missing. Recipients computed per event in `computeRecipients()`; the actor is always filtered out. Deep-link path picked per recipient's primary role via `LINK_PREFIX_BY_ROLE`.
- `notifyComment({ requestId, commentId, actor })` — fan-out to IC + HOG + PC + assigned faculty, in-portal channel only.
- `notifySlaReminder({ request, classification, ageHours, slaHours })` — used by the cron route. Recipients picked by `HOLDER_ROLE_MAP[status]`. Per-recipient dedup query against `Notification.meta` (`kind=sla.reminder` + `requestId` + `classification`) within a `slaHours/2` window.

The whole module is best-effort: all delivery happens inside `Promise.allSettled`, the top-level `notifyTransition` wraps its body in try/catch, and `deliver()` writes a `Notification` row first then attempts SMTP — a failed email leaves the row at status `FAILED`, never throws.

Channels: `IN_PORTAL` writes a row with status `SENT` immediately (no transport). `EMAIL` calls `sendMail()` from `@hmp/integrations` and updates status based on result.

**Handout structured data (BITS schema).** Handout content is validated by Zod schemas in [packages/db/src/handout-schema.ts](../packages/db/src/handout-schema.ts). Schema versions are explicit (`LATEST_SCHEMA_VERSION`, currently 1) and never silently upgraded — `BitsHandoutSchema` is a discriminated union on `schemaVersion`. `HandoutVersion.data` (added Prompt 11a, nullable) is the new source of truth for handout content; `contentHtml`/`contentJson` are legacy fields kept for backward compatibility with pre-Prompt-11 handouts. The editor (Prompt 11d) and renderer (Prompt 11c) build on this schema; the golden-file fixture transcribes a real BITS lab handout (`AEL ZG631`) faithfully.

**Empty-array & optional-field allowances (BITS schema).** The schema is deliberately permissive where real handouts legitimately omit a field — but only where backed by a specific source handout, never inferred from one sample, and never relaxed merely to chase a corpus pass rate:

- `experientialLearning` is `.optional()` — a genuine theory course (`CC ZG501`) has no experiential section at all. When present, its inner arrays (`components`, `experiments`, `overallScope`, `labInfrastructure`) may all be empty (a course that keeps the header but lists no components).
- `partA.referenceBooks` may be empty — some handouts list no reference books.

The principle: don't fabricate a value to satisfy the schema, and don't relax the schema to chase a number. If a representative real handout legitimately omits a field, the field becomes `.optional()`; the gap between "the proxy sweep can't find a section" and "the handout lacks it" is an extraction problem for the 11f parser, not a reason to loosen the schema (see §5 "Known parser challenges for Prompt 11f").

**BITS course code (canonical / alternates / legacy).** Added Prompt 11b. The canonical identifier is `Course.bitsCourseNumber` (one ASCII space between a 2-4 letter discipline prefix and the `Z[CG]nnn` cluster — e.g. `"AE ZG631"`). Validated and normalized via [`normalizeBitsCourseNumber`](../packages/db/src/course-code.ts) (handles the four real-world transcription irregularities found in the 11b corpus survey — joined, run-split, mid-digit-split, lowercase). The strict post-normalization Zod gate is [`bitsCourseNumberSchema`](../packages/db/src/course-code.ts); cross-listed and theory+lab-paired courses carry equivalent codes in `alternateCodes: String[]` (33% of sampled corpus files have ≥2 codes — `MBA ZC417/PDBA ZC417/PDFT ZC417`, `AE ZC442/AEL ZC442`, `CSI ZC447/ES ZC447/IS ZC447/SS ZC447`). The legacy free-form `Course.code` field is kept `@unique` and double-written equal to `bitsCourseNumber` for back-compat with the CSV importer and admin Course-create action; **to be dropped in a follow-up cleanup PR** once every caller migrates. Discipline (`"MBA"`, `"SE"`, etc.) is **derived**, not stored — [`getDiscipline(course)`](../packages/db/src/course-code.ts) returns the canonical prefix; no sync column to fall out of date. The CSV importer and admin `createCourseAction` both **reject** invalid codes with an educational error naming the canonical example — see Prompt 11b Decision 4.

**Field transformation: title casing.** Some corpus titles arrive ALLCAPS (`MANAGERIAL ECONOMICS`, `POWER PLANT ENGINEERING`, `MICROPROCESSORS AND MICROCONTROLLERS`). The seed and the 11f importer Title-Case them for readability — **one-way editorial transformation**, same pattern as the 11a `subTopics` join/split contract. Acronyms within a title (e.g. an OEM name or a registered course name in caps) are preserved verbatim. The renderer (Prompt 11c) outputs whatever is in the database — no reversal needed because the canonical display form lives in the column.

**BITS-format renderer (Prompt 11c).** BITS handouts are rendered via [`renderBitsHandout`](../packages/db/src/handout-renderer.ts) in `@hmp/db`. **Pure function**: structured `BitsHandoutV1` in, HTML string out. No I/O, no Prisma, no React. Importable from both server and client. Used by every read path (SME nominations, faculty preview, IC/PC/HOG request pages) AND by the Mode B Taxila export ZIP builder — **the renderer is the single source of truth for BITS-format HTML; do not duplicate rendering logic in consumers.** Every rich-text field (`courseDescription`, `laboratoryComponent`, `experientialLearning.overallObjective`, `evaluation.midSemSyllabus`/`compreSyllabus`/`notes`, `evaluationGuidelines`) flows through the SAME `sanitize()` helper backed by `sanitize-html` with a tight allowlist sourced from the SHARED constants [`BITS_RICH_TEXT_ALLOWED_TAGS`](../packages/db/src/rich-text-allowlist.ts) (`p`, `br`, `strong`, `b`, `em`, `i`, `u`, `ul`, `ol`, `li`, `a` with restricted `href` schemes, `span`, `code`) — `<script>`/`onerror`/`javascript:` are stripped uniformly across all four fields. The structured editor's TipTap config (Prompt 11d, [`bits-tiptap-extensions.ts`](../apps/web/src/lib/bits-tiptap-extensions.ts)) consumes the SAME allowlist, so drift between editor capability and renderer-stripped output is mechanically impossible. The companion helper [`resolveHandoutHtml`](../packages/db/src/handout-display.ts) is the canonical "structured `data` preferred; legacy `contentHtml` fallback" decision: both the 5 in-app read paths AND the Taxila Mode B builder call the SAME helper, so the eventual cleanup PR that drops `contentHtml` is a one-file edit. The companion `sub-topic` rendering rule: `>1` items → `<ul><li>`; `==1` → inline; `==0` → empty cell (preserves clean empty Review sessions).

**Derived-column invariant: `HandoutVersion.contentHtml` is rendered from `data`.** Structured handouts maintain a derived `contentHtml` column rendered from `data` via `renderBitsHandout()`. The invariant: every write to `HandoutVersion.data` via the app's helpers (currently [`appendStructuredVersion`](../apps/web/src/lib/handout-versioning.ts)) re-renders `contentHtml` in the SAME transaction. Direct DB updates to `data` without re-rendering `contentHtml` create staleness — legacy consumers reading `contentHtml` see stale HTML, the SME / IC / PC / HOG pages and the Mode B export ZIP all serve the wrong content until the next app-driven save. **If you ever need to bulk-update `data` outside the app (data migration, repair script), re-render `contentHtml` in the same operation.** The legacy `appendVersion` (TipTap path) writes its own derived `contentHtml` via `renderTiptapToHtml`; the structured `appendStructuredVersion` writes via `renderBitsHandout`. Both columns share the invariant but use different renderers based on which authoring path produced the version.

**Structured editor (Prompts 11d-a + 11d-b).** Faculty author handouts via section-based React forms ([`apps/web/src/app/faculty/assignments/[id]/structured-editor/`](../apps/web/src/app/faculty/assignments/%5Bid%5D/structured-editor/)) backed by a single `BitsHandoutV1`-shaped `useState`. Save persists via [`appendStructuredVersion`](../apps/web/src/lib/handout-versioning.ts) (writes `data`, renders `contentHtml`, leaves `contentJson` null — `HandoutVersion.contentJson` is nullable for this path). The four rich-text sub-fields use [`BITS_TIPTAP_EXTENSIONS`](../apps/web/src/lib/bits-tiptap-extensions.ts) — TipTap config limited to the renderer's allowlist via the shared constants. **Legacy TipTap editor stays accessible** for pre-Prompt-11d handouts (`data: null`); the faculty page routes to the structured editor if `data` is present, otherwise shows the legacy editor with a [`ConvertBanner`](../apps/web/src/app/faculty/assignments/%5Bid%5D/structured-editor/ConvertBanner.tsx) above it. Conversion is one-way: clicks the [`convertToStructuredAction`](../apps/web/src/app/faculty/assignments/%5Bid%5D/structured-actions.ts) which pre-populates Part A metadata from server-derived request context (course title, course numbers from `bitsCourseNumber + alternateCodes`, instructor name, semester) and leaves content sections blank — **no parsing of legacy `contentHtml` back into structured fields**.

Prompt 11d-b enriched the complex sections: Part B sub-topics + references use chip-list editors with typeahead (references pull suggestions from Part A's T/R codes); Evaluation Scheme has a live 100% sum validator with the Save button blocked when total ≠ 100; Experiential Learning has full nested repeats for components, scope/lab-infrastructure bullet lists, and experiments. A structured AI-draft adapter ([`generateStructuredHandoutDraft`](../packages/ai/src/structured-handout-generator.ts) + [`StructuredAiDraftDialog`](../apps/web/src/app/faculty/assignments/%5Bid%5D/structured-editor/StructuredAiDraftDialog.tsx)) generates a `BitsHandoutV1` directly from course context; uses TypeScript-as-DSL + a compact AEL-ZG631 example for the prompt; falls through to a Zod-valid stub when the AI provider is unconfigured.

**Chip-list pattern (Prompt 11d-b).** Repeating string-array fields use **two** shared primitives at [`structured-editor/chips/`](../apps/web/src/app/faculty/assignments/%5Bid%5D/structured-editor/chips/):

- [`ChipList`](../apps/web/src/app/faculty/assignments/%5Bid%5D/structured-editor/chips/ChipList.tsx) — horizontal pills, Enter-to-add, optional `suggestions` for typeahead, optional `warnIf` for soft validation (used for Part B sub-topics + references).
- [`BulletList`](../apps/web/src/app/faculty/assignments/%5Bid%5D/structured-editor/chips/BulletList.tsx) — vertical list, semantic `<ul>` model, one input per row (used for experiential `overallScope` + `labInfrastructure`).

Two named components beat one configurable `mode: 'chip' | 'list'` monster — the visual model genuinely differs (chips for short tags, bullet lists for sentence-length items) and call sites read better when intent is in the type name. Future repeating-array surfaces should pick the right one rather than introduce a third variant. If a field needs richer formatting than a `string`, the schema field should be promoted (e.g. to `{ text, ref }`) — don't extend the chip primitives into objects.

**Evaluation Scheme 100% rule (UI-only).** Prompt 11d-b's load-bearing business rule: sub-component weights must sum to 100 across all ECs. **Enforced by the editor layer**, not by the Zod schema — `BitsHandoutSchemaV1` allows any individual weight 0-100 and any number of components. The pure helper [`isEvaluationValid`](../apps/web/src/app/faculty/assignments/%5Bid%5D/structured-editor/sections/evaluation-validity.ts) computes the total; `EvaluationScheme` reports validity via `onValidityChange`; the root editor disables Save / Submit when invalid. **If you ever need to validate the rule server-side** (e.g. an importer that produces handouts directly), the same `isEvaluationValid` helper is importable — keep the rule in one place.

**Notification recipient categories (event vs task).** When adding a notification, decide which category it is — the actor-inclusion rule differs:

- **Event notifications** ("X happened") — exclude the actor; they caused the event and don't need to be told. Examples: `notifyComment`, `notifyManuallyPublished`, `notifySme*`.
- **Task notifications** ("you need to do Y") — _include_ the actor; they're the one who must act on the pending task. Example: `notifyPublishExportReady` (the IC who exported needs the "download → upload → confirm" reminder).

Copying a notify function from another is fine, but name the category first — "both are notifications" is not enough (this exact slip shipped a self-notification bug caught only by CI; PR #9).

**Async queue vs synchronous (workers).** Notifications and the AI quality report route through the BullMQ queue boundary (`dispatchOrEnqueue` in notifications.ts) when `WORKERS_ENABLED=true`, else run inline. Three routing categories make the rule complete:

1. **Void fire-and-forget side-effects** → queue when `WORKERS_ENABLED` (e.g. the 8 `notify*` void functions, the quality report on submit).
2. **Value-returning notifications** (return a count / summary / id the caller consumes) → **never queue** — the return value is the response contract. `notifySlaReminder` returns the count both the cron route and the admin manual-sweep accumulate (`notified += await …`); queueing would report zero.
3. **User-triggered "do this now" actions** where the result IS the response → **never queue**. `regenerateRecommendation` — the HOG waits on the recommendation it returns.

When adding a notify/action, pick the category and the queueing decision is forced. See [@hmp/queue](../packages/queue/) and `apps/web/src/workers/`.

**Queueing is a property of the calling context, not the function.** The same function can be category 1 in one caller and category 3 in another — only the caller decides whether to enqueue. Canonical example: `runQualityReport` runs **synchronously** when the faculty clicks "Run quality check" (`runQualityCheckAction` — they await the score) and is **queued** as a fire-and-forget side-effect of submit (`submitForReviewAction`, when `WORKERS_ENABLED`). Don't bake the queue decision into the function; make it at the call site.

**Splitting large changes (mechanical-then-behavioral).** A large change that includes a risky behavior modification is split into two PRs: ship the mechanical/safe half first (a new engine/package/refactor with _no behavior change_, zero production callers), then the behavioral half second (wiring, UX, the new behavior) with a small focused diff. Each PR is reviewable in isolation and the behavioral half is independently revertible. Precedents: PRs #8/#9 (Taxila stub → real), #10a/#10b (synchronous → queued workers).

**Additive-first / destructive-second at the PR boundary (Prompt 12-a/12-b).** When refactoring a load-bearing system that has existing consumers (workflow engine, a shared model, an enum everything switches on), the ADDITIVE PR ships first — new structures alongside the old, new code paths opt-in, nothing removed or retargeted — and the DESTRUCTIVE PR ships second — remove the old structures, retarget consumers, flip defaults, rewrite the tests that asserted the old behavior. The discipline test: **every existing test must stay green at the additive PR's boundary** (no test rewrites needed yet), because nothing existing changed behavior. If landing the "additive" PR forces you to rewrite existing tests or touch consumer UI, the boundary is wrong — work has leaked across it. Model: 12-a added `SmeAssignment` + `SME_REVIEW` status + 3 events + opt-in submit routing while leaving `SmeNomination` + the advisory UI fully working; 12-b dropped `SmeNomination`, removed the advisory code, made SME mandatory, and retired the m4b/m4c/m4d advisory E2E (replaced by a new `m4-sme-approval-loop` spec). This is stricter than the mechanical-then-behavioral split above (which allows the first PR to have zero callers); here the first PR ships REAL new behavior, but only on an opt-in path that no existing test exercises. The Prisma migration discipline mirrors it: additive enum `ADD VALUE` + new table in the first migration; destructive `DROP TABLE` in the second.

**Tests that need a real external service are probe-skipped locally, run in CI.** Some integrations can't be faithfully mocked: BullMQ needs real Redis (ioredis-mock implements neither Lua scripts nor the blocking commands BullMQ relies on); Mode B export needs MinIO; email-delivery assertions need Mailhog. The pattern: split each suite into **pure-logic tests** (no service — always run) and **integration tests** that probe the service at startup and `skipIf` it's unreachable. CI provides the service so the integration half actually runs; a Docker-less dev box runs the pure half and skips the rest. Don't burn time trying to mock these — reach for the probe-skip. Precedents: MinIO (m6a/m6c), Mailhog (m4d/m6c email), Redis (`@hmp/queue` integration tests).

**Vitest mock hoisting.** When mocking modules imported by the file under test (Prisma, the AI client's `chatJson`, etc.), the mock values closed over by the `vi.mock()` factory must be declared with `vi.hoisted()`. Vitest hoists `vi.mock()` calls above all `import` statements, so a plain `const mockX = vi.fn()` at the top of the test file isn't yet initialized when the factory runs — referencing it throws `ReferenceError: Cannot access 'mockX' before initialization`. Pattern: `const { prismaMock, chatJsonMock } = vi.hoisted(() => ({ prismaMock: { ... }, chatJsonMock: vi.fn() }))` BEFORE the `vi.mock()` calls; the factory then references the hoisted names safely. Real imports go after the mocks. Precedents: [`packages/ai/src/structured-handout-generator.test.ts`](../packages/ai/src/structured-handout-generator.test.ts), [`apps/web/src/workers/__tests__/processors.test.ts`](../apps/web/src/workers/__tests__/processors.test.ts).

**Synthetic test fixtures must be validated structurally against real source files before being trusted (Prompt 11f-b1).** When parsing external data formats (e.g. `.docx` corpus files), the synthetic fixtures used in unit tests must mirror the shape of real source files — NOT just whatever shape the fixture-generation library produces by default. Without validation, fixtures encode the test author's assumptions; tests pass against those assumptions; the parser then fails in production against the actual data shape. 11f-a learned this the hard way: synthetic `.docx` fixtures generated via the `docx` library produced "label-paragraph-then-table" structure, but real BITS corpus `.docx` (via mammoth's HTML output) has "label-cell-as-first-row-of-table", no-header-row T/R tables, suffix-bearing LO labels, paragraph-form Course Description, rowspan'd Evaluation EC cells, and bullet-list sub-topic cells. The 95.3% MAMMOTH_STRUCTURED rate reported in 11f-a was misleading — every import had placeholder warnings for CO/LO/T-books/Evaluation because the parser's table finders, trained on the synthetic shape, missed the real shape entirely. The fix: before writing a fixture, read at least one real source file's raw output (e.g. `mammoth.convertToHtml(realFile).value`) and design the fixture to match. The `f6-real-corpus-shape.docx` fixture added in 11f-b1 is the precedent — its docstring documents which real-corpus differences it exercises. Section §5 below documents the 11f-a → 11f-b1 corrections in detail.

### Corpus import (Prompts 11f-a + 11f-b1 + 11f-b2)

The corpus parser ([packages/db/src/corpus-import/parser.ts](../packages/db/src/corpus-import/parser.ts)) converts a BITS WILP `.docx` handout into a `BitsHandoutV1` via three tiers, in order:

1. **`MAMMOTH_STRUCTURED`** — mammoth converts the .docx to HTML preserving tables and joining adjacent runs; a minimal regex tree walker parses paragraphs and tables; labeled-table finders locate the Part A header / CO / LO / T-book / R-book / Part B / Evaluation tables and project them into the schema. **86%** of the 384-handout real corpus lands here.
2. **`TEXT_FALLBACK`** — when Tier 1's structured pass can't find the Part A header (e.g., the table layout is degenerate), fall back to `mammoth.extractRawText` + regex matching for at least the course number, so the admin UI surfaces the row with `data: null` and a "manual re-import recommended" warning.
3. **`FAILED`** — both tiers couldn't recover; row written with `data: null`, specific reason in `parseErrors`. Rare in practice; the real-corpus run leaves 6 of 418 files here (3 EE, 2 POM, 1 ST) — all "Part A header table not found" cases that suggest those families use a layout the survey didn't sample.

Pre-flight skips don't run either tier:

- `SKIPPED_FORMAT` — legacy `.doc` binary (33 files) and 1 `.pdf` (`ES ZG614`). Mammoth doesn't read either; libreoffice conversion is out of scope.
- `SKIPPED_SIZE` — files >3 MB (84 files). Image-heavy outliers; 11a precedent kept.
- `SKIPPED_MODULE` — EE-style "Course Modules" template (9 files after 11f-b1: EE ZG511/513/514/521/523/614/621/622 + PE ZC321). EE_ZG513 was missed by 11f-a's strict `/^course modules$/i` detector and 11f-b1 relaxed it to `/\bcourse modules\b/i`. Per-module Objectives don't map to `partA.courseObjectives`; 11f-b2 will decide whether to extend the schema or fold modules into existing fields (honest-empty mapping recommended).
- `SKIPPED_NARRATIVE_PROSE` — Survey B finding in 11f-b1. 5 corpus files (EE ZG613, EE ZG623, POM ZG512, POM ZG522, ST ZG612) use a "narrative-prose" template: Part A as colon-separated lines, text books as un-tabled prose lists. Parser detects via the `Course No\. : <code>` paragraph signature and returns with the course number extracted; mapping to BitsHandoutV1 is parser-design work for a future prompt.

**HHSM value-swap detection** lives in `extractPartAHeader`. When the "Course No(s)" cell value fails `normalizeBitsCourseNumber()` AND the "Course Title" cell value succeeds, swap them and emit a warning. **Generalizes beyond HHSM** — the real-corpus run caught 16 files across HHSM, MBA, BA, PDFI families.

**Synonym map correction (corrects an earlier §5 claim).** `Experiential Learning` ≡ `Laboratory` is real (ES_ZC424 confirmed). `Modular Content` is **NOT** a synonym — survey-confirmed: DE_ZC415 has "Modular Content Structure" as a syllabus expansion (not lab/practical); CC_ZG506 and SE_ZG587 have BOTH labels at different document positions. Parser drops Modular Content content with a `parseWarnings` entry per the audit-§5 data-quality-report discipline.

**Required-but-missing field policy.** When the source has blank cells for required schema fields (notably `creditModel.description`), the parser provides an empty-string placeholder and emits a `parseWarnings` entry. This is the explicit audit-§5 data-quality-report layer: schema is representational; the parser doesn't fabricate values; admin UI surfaces the warning so faculty can fill in the missing data before submitting. **Applies to**: creditModel, CO/LO/T-book placeholder rows when those tables are missing.

**Idempotency.** `runCorpusImport` upserts on `sourceFile` (the file's canonical path, `@unique` in the schema). Re-running on a file with unchanged `(bytes, mtime)` is a no-op (0.4s for 418 files in practice). A changed file forces re-parse via the upsert path. **Admin's approval state (`approvedForReuse`, `approvedAt`, `approvedById`) is preserved across re-imports** — only the parser-output columns are updated.

**Synthetic fixtures over real corpus samples** (`packages/db/src/__fixtures__/corpus-samples/`). Seven hand-constructed `.docx` files via `packages/db/scripts/generate-corpus-fixtures.ts` cover the parser code paths (f1 standard happy path, f2 HHSM swap, f3 Module template, f4 Modular Content, f5 malformed, f6 real-corpus shape, f7 narrative-prose). f6 was added in 11f-b1 specifically to mirror the AEL_ZG631 golden's real-corpus structure that 11f-a's fixtures didn't exercise. Regenerable via `pnpm --filter @hmp/db fixture:generate`. Real corpus samples even gitignored carry IP risk; synthetic fixtures let tests run in CI and document parser coverage in the script itself.

**11f-b1 parser correctness fixes (audit §5 has the diff against 11f-a).** Five real-corpus structural differences my synthetic fixtures didn't exercise, surfaced by Survey C's warning-count analysis (286 imports all carried placeholder warnings for CO/LO/T-books/Evaluation):

1. **CO/LO/T/R tables with no header row.** Real corpus T-book / R-book tables start directly with `T1 | citation` rows (no `Code | Reference` header). Original parser sliced off the first row blindly; 11f-b1 introduced adaptive `firstRowIsData` logic: peek at the first cell; if it doesn't look like a header (`/^(code|no|s\.?\s?no)$/i`), prepend a synthetic header so `.slice(1)` keeps the real data row.
2. **CO header uses "No" not "Code".** Header regex relaxed from `/^code$/i` to `/^(code|no\.?)$/i`.
3. **LO label has " : Students will be able to" suffix.** Anchored `$` removed from the label regex.
4. **Course Description as a `<p><strong>Course Description: </strong>prose</p>` paragraph** outside the Part A header table. Parser falls back to a paragraph-scan after exhausting the header table.
5. **Code-shape filter on extracted rows.** Real corpus tables sometimes interleave non-coded rows (totals, multi-line continuations); the parser now filters each extracted row to require `code` matches `/^CO\d+$/` / `/^LO\d+$/` / `/^T\d+$/` / `/^R\d+$/` and silently drops the rest. Prevents Zod failures on otherwise-good imports.

Plus three Part B / Evaluation fixes:

6. **Part B header is "Contact Session | List of Topic Title | Sub-Topics | Reference"** — accept both "Topic Title" and "List of Topic Title".
7. **Evaluation header is "Evaluation Component | Name | ..."** (real corpus) — accept both "EC No." (synthetic) and "Evaluation Component" (real).
8. **Evaluation EC cells use HTML `rowspan="2"`** when an EC has multiple sub-components — second-row data appears with one fewer cell. Parser detects rows whose first cell isn't an EC code and inherits the previous EC, shifting cell indices.
9. **Sub-topics rendered as `<ul><li>X</li><li>Y</li></ul>`** within table cells. Parser inserts `"; "` between `<li>` boundaries before tag-stripping, preserving the bullet structure per the schema's canonical `"; "` join contract.

**11f-b2 parser fixes (Survey D — second wave of real-corpus correctness work).** Survey D revealed three more shapes the 11f-b1 parser missed:

10. **Eval header is "No | Name | Type | Duration | Weight | Day, Date, Session, Time"** in most real corpus files. Two issues vs synthetic fixtures: col0 is "No" (not "Evaluation Component" or "EC No."); **column order is Duration BEFORE Weight** (synthetic had Weight before Duration). `parseEvaluationTable` now detects column positions from the header row instead of hard-coding indices.
11. **Part B header is "Contact Hour | List of Topic | Sub-Topics | Reference"** in the AEL/AE family (not "Contact Session"). Header regex accepts both. Also supports "Content Structure:" label paragraph as a label-after-paragraph fallback for QM-style files.
12. **Module-template Part A** uses "Course ID No." (not "Course No(s)"), "Lead Instructor" (not "Instructor"), "Academic Term" (not "Date"). `extractPartAHeader` accepts both label sets; `findPartAHeaderTable` accepts either form when locating the Part A table.

**11f-b2 Module-template honest-empty mapping (replaces 11f-b1's SKIPPED_MODULE).** The Module template (8 EE files + PE ZC321 — 9 of 384) fundamentally lacks Course Objectives and Learning Outcomes in source (per-module Objectives only). Per the planner decision, the 11f-b2 parser produces a Zod-valid `BitsHandoutV1` with:

- `partA.courseObjectives = []` + parseWarning naming the source gap
- `partA.learningOutcomes = []` + parseWarning naming the source gap
- `partA.courseDescription`, T-book, R-book, Evaluation extracted from the Module template's standard sections
- `partB.sessions` populated from the per-module "Self-Study & Contact Session Plan" tables (each `Topic No. | Topic Title | Reference` row → one session, with the preceding "Module Title:" used as the topic prefix)

**Schema relaxation (Prompt 11f-b2 — corpus discipline correction).** `BitsHandoutSchemaV1.partA.courseObjectives` and `partA.learningOutcomes` had `.min(1)` constraints in 11a. Surveyed Module-template files genuinely lack these sections in source; corpus-validated discipline (audit §5) says the schema follows the data. Both arrays now accept empty (the schema change is backward-compatible because all existing rows have length ≥ 1).

**Submit-time CO/LO validation (UI-only enforcement, schema permissive).** Empty CO/LO arrays are allowed in `saveStructuredDraftAction` (drafts WIP — faculty can save a Module import partially-filled across sessions) but rejected in `submitStructuredForReviewAction` with a faculty-facing message naming the missing section. The editor's Submit button is disabled with a `title` tooltip when either array is empty. Same shape as the evaluation 100% rule (UI/action enforcement, schema representational). The discipline: schema represents what _can_ exist; UI enforces what _should_ exist at submit time.

**Approval workflow (Prompt 11f-b2).** Admin curates which imports surface to faculty via Tier 2 auto-fetch. Two paths:

- **Bulk-approve.** Two-step server actions: `countBulkApproveEligibleAction()` returns the eligible count + first 10 course numbers (no mutations) for a confirmation dialog. `bulkApproveEligibleAction()` does the actual UPDATE. Eligibility filter: `extractionMethod = MAMMOTH_STRUCTURED AND parseWarnings.length <= 1 AND bitsCourseNumber IS NOT NULL AND approvedForReuse = false`. The two-step shape catches state-sync drift between dialog open and confirm — the action result reports the count actually updated; the dialog count was pre-flight.
- **Per-row.** `approveImportAction(id)` / `rejectImportAction(id)` for individual rows. Approve has no eligibility gate — admin judgment overrides the bulk filter. Reject deletes the row.

**Course-row reconciliation (Option 3 — Tier 2 queries HandoutImport directly).** The auto-fetch Tier 2 query doesn't require a matching `Course` row. Imports surface based on `bitsCourseNumber` overlap regardless of whether a Course row exists. The admin UI shows a "Course row not found — create it?" link per import that lacks a match; the link deep-links to `/admin/programmes?prefillCode=…&prefillTitle=…` with the form pre-filled. Three independent actions (approve, create-course, neither) each one click.

**Tier 2 banner detail format** (`apps/web/src/lib/handout-auto-fetch.ts`): `Imported corpus handout: <courseCode>` with optional `(Module format — review CO/LO sections)` suffix when the source import has the "Module template source" parseWarning. Faculty sees the source course identity + a Module-specific hint when applicable.

### Auto-fetch cascade for new handouts (Prompt 11e + Tier 2 wiring in 11f-b2)

When faculty clicks "Start editing", `startEditingAction` walks a three-tier cascade to pre-populate the structured editor's initial version. Implementation in [`apps/web/src/lib/handout-auto-fetch.ts`](../apps/web/src/lib/handout-auto-fetch.ts) (pure resolver + DB wrapper + Tier 2 stub).

**Tier order:**

1. **Prior version** — the most-recent `HandoutVersion` whose handout is `PUBLISHED` or `ARCHIVED` and whose course matches the current course by symmetric overlap on `bitsCourseNumber + alternateCodes` (Postgres array `&&`, expressed via Prisma's `hasSome`). Drafts in progress (`IN_PROGRESS`, `SUBMITTED`, `UNDER_REVIEW`, etc.) are intentionally excluded so faculty A can't see faculty B's WIP just because they share a course code. The cascade-loaded data goes through `stripIdentifiersForCarryForward` before write: `partA.date` → today, `partA.instructors` → current faculty, `metadata.semester` → current semester, `metadata.formNumber` → `''`, `partA.courseNumbers` → current course's `[bitsCourseNumber, ...alternateCodes]`. **Content is preserved** (objectives, books, sessions, evaluation including weights and the 100% total) — faculty must review and confirm. The banner reminds them: if grading policy changed institutionally, the inherited weights are the place to update.
2. **Corpus import** — stub returning `null` until Prompt 11f ships the `HandoutImport` table. Tier 2's eventual real query mirrors Tier 1's symmetric overlap, restricted to imports marked "approved for re-use" (the flag 11f introduces). The cascade falls through to Tier 3 in 11e's world.
3. **Empty template** — `blankHandoutForRequest(ctx)` from the structured editor's state module. Same Zod-valid placeholder data the legacy `convertToStructuredAction` produces.

**Product shift to flag:** before 11e, a fresh handout's v1 was a TipTap document from the `Template` table, and faculty had to click "Convert to structured" before seeing the structured editor. Post-11e, every new handout's v1 is structured `BitsHandoutV1` data and the structured editor opens directly. The legacy `Template` table + `ConvertBanner` + `convertToStructuredAction` are still wired for any pre-11e legacy versions still in production (they remain reachable when `currentVersion.data` is `null`), but no new handout flows through that path.

**Symmetric cross-listing match.** A prior course matches the current course when **any** code in current's `[bitsCourseNumber, ...alternateCodes]` set appears in prior's `[bitsCourseNumber, ...alternateCodes]` set. The query uses one merged `handout: { status: { in: [...] }, request: { offering: { course: { OR: [...] } } } }` clause — NOT two `handout:` keys (the second would silently overwrite the first and drop the status filter, leaking drafts-in-progress into the cascade). Regression-guarded by [`apps/web/src/lib/handout-auto-fetch.test.ts`](../apps/web/src/lib/handout-auto-fetch.test.ts)'s "builds a single merged handout clause" test.

**Banner state via URL search params, not a persisted marker.** After `startEditingAction`, the server `redirect()`s to `/faculty/assignments/[id]?autoFetched=<tier>&detail=<urlencoded>`. The page server component reads `searchParams` and passes the parsed `{ tier, detail }` to `<StructuredEditor>`, which renders `<AutoFetchBanner>` above the tab bar. Dismissing the banner (or clicking "Start from empty template instead") calls `router.replace(pathname)`, dropping the params — the banner doesn't show again on subsequent visits. **Banner-shows-once** is intentional UX: faculty acknowledged the source on first view; no need to keep reminding them. No schema change, no notes-field overload.

### SME approval gate (Prompt 12-a; 12-b completes it)

The SME (Subject Matter Expert — a department head) is an approval gate between faculty submit and PC review, replacing the old advisory-only SME concept (PRs #5–#7). Workflow shape:

```
IN_PROGRESS --SME_REVIEW_REQUESTED[Faculty]--> SME_REVIEW
SME_REVIEW  --SME_APPROVED[SME]--> SUBMITTED          (PC's EXISTING review queue)
SME_REVIEW  --SME_REVERTED[SME]--> REWORK_REQUESTED   (faculty's EXISTING rework state)
REWORK_REQUESTED --SME_REVIEW_REQUESTED[Faculty]--> SME_REVIEW   (indefinite loop)
```

Load-bearing facts:

- **`SME_REVIEW` sits BEFORE `SUBMITTED`, not after.** `SUBMITTED` is PC's queue and `UNDER_REVIEW` is HOG's queue (this predates SME). Routing SME approval to `SUBMITTED` keeps the entire `SUBMITTED → UNDER_REVIEW → APPROVED` chain — and the PC/HOG dashboards' status filters — byte-for-byte unchanged. The naming inversion (`SME_APPROVED` event yields the `SUBMITTED` status) is intentional and documented at the transition site in [machine.ts](../packages/workflow/src/machine.ts); the SME approval IS the handoff to PC review.
- **SME is always the gate.** `SME_REVIEW_REQUESTED` fires from BOTH `IN_PROGRESS` (first submit) and `REWORK_REQUESTED` (re-submit) — so a handout that a PC sent back for rework re-routes through the SME on re-submission, not straight back to the PC. Conscious choice: SME re-checks every submission.
- **One SME per handout**, assigned by the HOG at allocation (`SmeAssignment.requestId` is `@unique`). The SME for one handout serves many handouts (department heads have broad portfolios).
- **Iteration history lives in `Approval(stage=SME_REVIEW)` rows**, one per decision (`decision=APPROVED` on approve, `REWORK` on revert), NOT on `SmeAssignment` (which carries no status/timestamp columns — it's just the "who is the SME" pointer). This mirrors how PC/HOG decisions already record as `Approval` rows; cross-stage audit queries naturally include SME decisions.
- **SME revert comments reuse the existing per-`Handout` `Comment` thread** (author = SME). Faculty see them in the same thread as PC rework comments; the comment author's role distinguishes them. No new comment model.
- **Opt-in routing in 12-a, mandatory in 12-b (now).** The submit actions ([structured-actions.ts](../apps/web/src/app/faculty/assignments/%5Bid%5D/structured-actions.ts), [actions.ts](../apps/web/src/app/faculty/assignments/%5Bid%5D/actions.ts)) require an `SmeAssignment` and route every submit to `SME_REVIEW_REQUESTED` — a missing assignment now blocks submission with a faculty-facing error instead of falling back to `SUBMITTED`. (In 12-a this was opt-in: present → `SME_REVIEW_REQUESTED`, absent → legacy `SUBMITTED`, which kept every pre-12-a E2E green because their fixtures allocated no SME. 12-b removed that fallback + made `smeUserId` required at allocation.)
- **Verification reset**: the dev DB accumulated 149 in-flight `HandoutRequest` rows + 3 `SmeNomination` rows during template-stack development (E2E/test artifacts). 12-a's local verification ran `prisma migrate reset` + reseed to start the SME walkthrough from clean seed data; safe because nothing is deployed and the corpus imports are regenerable via the import action. (12-b later dropped the `SmeNomination` table entirely via the `drop_sme_nomination` migration.)

### How audit rows are written

Two paths:

- Workflow audit — automatic, inside `transition()`. Action string is `handout.transition.${event}`.
- Non-workflow audit — explicit `audit({...})` call from [apps/web/src/lib/audit.ts](../apps/web/src/lib/audit.ts). Used for `request.create` (before the first transition), `assignment.accepted`, `handout.version.saved`, `ai.recommend.regenerate`, `ai.quality.generated`, and admin CRUD. Caller supplies `actorId`, `action`, `entity`, `entityId`, optional `before/after/requestId/ip/userAgent`.

`before`/`after` are stored as JSON; both default to `null` if omitted. `requestId` links the row back to a `HandoutRequest` for the audit page query.

### How RBAC is enforced

Three layers, in order:

1. **Middleware** ([apps/web/src/middleware.ts](../apps/web/src/middleware.ts)) — `auth((req) => ...)` from NextAuth. Redirects unauthenticated users to `/login`. Does not check role; only presence of a session.
2. **Server-action / RSC** — `requireRole(await getSessionUser(), RoleName.X)` from `@hmp/auth` ([rbac.ts:41](../packages/auth/src/rbac.ts)). Throws `AuthorizationError` (status 403) if role missing. `ADMIN` is a super-role and bypasses every `hasRole` check ([rbac.ts:31](../packages/auth/src/rbac.ts)).
3. **Workflow** — `assertRoleAllowed(event, roles)` inside `transition()`. Even if a server action somehow lets the wrong role through, the workflow layer refuses the state change.

Permission keys (`requirePermission`) exist but are not exercised by the action files audited; current role gates are role-based, not permission-based. Permission strings live on the JWT (loaded in `loadUserAccess`) so the plumbing is ready when finer-grained gates are added.

### Bulk CSV upload (Prompt 13)

IC bulk handout-request creation (`/ic/requests/bulk-upload`) establishes the convention for role-scoped bulk CSV uploads (HOG allocate in 14, queue actions in 15 follow it):

- **Parse via the shared `@hmp/integrations` convention, not a new library.** `parseCsv` (RFC-4180-ish, header-keyed, trims BOM, skips blank + `#`-comment lines) + a per-kind `parseWith(input, zodSchema)` → `ParseResult<T> = { ok, rows, errors: [{ line, message }] }`. Add a `parseXxxCsv` wrapper alongside the existing four; do **not** add papaparse/csv-parse. Header-keying makes column **reordering** free; `#`-lines make commented template examples free.
- **Two validation layers.** Structural (the parser — required columns + non-empty, DB-less, lives in `@hmp/integrations`) then semantic (the server action — existence + dedup, against the DB). The semantic layer never lives in the parser package (no DB there).
- **Atomic reject-all.** Validate every row first (lookups only, zero writes). If ANY row fails → reject the whole upload with a per-row error list + a downloadable annotated CSV (`_status` uniformly `rejected`; `_error_code`/`_error_message` only on failing rows, so the user filters for non-empty `_error_code`). Re-uploading the fixed whole file is safe because dedup skips already-created rows.
- **Resolution is programme-first** for anything keyed by a programme-scoped unique. `Semester` is `@@unique([programmeId, name])` — name is **not** globally unique — so resolve programme → semester(programmeId, name) → course → offering(courseId, semesterId). A name-only semester lookup is a bug (it would resolve a same-named semester under the wrong programme).
- **In-file dedup** (`Map<naturalKeyId, lineNumber>`) catches two rows resolving to the same target within one file — the DB-existence check can't (neither exists yet). Report both line numbers.
- **DB dedup is a status-aware semantic predicate, not a constraint.** `HandoutRequest` has no `@@unique([courseOfferingId])` by design (re-requests after REJECTED/ARCHIVED). Skip if an active (`status notIn [REJECTED, ARCHIVED]`) request already exists. **Race window:** two concurrent uploads can both pass the existence check — acceptable for a single-IC bulk path; named, not closed.
- **Reuse the workflow engine for state changes.** The bulk insert creates rows at `DRAFT` (via the existing `createRequestWithRefNo` — sequential refNos work in one transaction because intra-tx reads see prior intra-tx inserts), then drives each `DRAFT → REQUESTED` through `transition(REQUEST_INITIATED)` post-commit rather than inlining the status flip. A row whose post-commit transition throws is reported as `strandedAtDraft` (the `partial` result state) — recoverable, not silently lost.
- **Bulk operations produce one audit row per affected entity** (`action: 'request.create.bulk'`), all sharing a `bulkSessionId` (a `crypto.randomUUID()` generated once per call, stored in each entry's `after` JSON). That id IS the grouping mechanism — admins query matching `bulkSessionId` values; no parent-child audit schema. If grouping ever becomes a usability problem, a follow-up PR introduces parent-child audit linkage; until then admin audit filters (date / actor / action) group at query time.

> **Known cross-CSV column-naming inconsistency in `@hmp/integrations` (not blocking).** The wrappers disagree on the prefix pattern: `parseCoursesCsv` uses `code`, `parseProgrammesCsv` uses `code`, `parseOfferingsCsv` uses `course_code`, and `parseHandoutRequestsCsv` uses `programme_code` / `course_code` / `semester`. Each CSV is internally consistent; the _cross-CSV_ pattern (`entity_field` vs bare `field`) is not. Pre-exists Prompt 13. Deferred on purpose — a one-shot consistency PR across all wrappers is cleaner than piecemeal renames, and is best done **after Prompts 14 + 15** establish the full column set. Revisit if the BITS WILP team wants uniform columns in user training material.

### Bulk allocation — cap + transition semantics (Prompt 14)

HOG bulk faculty + SME allocation (`/hog/requests/bulk-allocate`) extends the bulk-CSV pattern to **existing** requests (addressed by refNo) and adds two conventions:

- **Faculty allocation rules (the off-campus cap, type-specific gating) derive from `User.facultyType`, not from per-allocation overrides.** A CSV upload has no authority to bypass them — there is no `is_off_campus` column; capping is intrinsic to the faculty member (`OFF_CAMPUS`/`ADJUNCT`/`GUEST` are capped, via the exported `isCappedFacultyType()` in `@hmp/workflow`). This makes the cap universal regardless of upload channel. The cap is **per-semester**, so the bulk cumulative check is keyed by `(facultyId, semesterId)` — the same off-campus faculty across two semesters does not accumulate against itself — seeded by one batched `findMany` (no N+1) and projected across the CSV rows.
- **Bulk operations that drive workflow transitions reuse the per-row `transition()` API.** `transition()` always opens its own `prisma.$transaction` (no external-tx parameter), so each row is **individually** atomic; the bulk operation as a whole is **not**. The 3-state response (`success` / `partial` / `rejected`) communicates this: `partial` = some rows committed, a later row's transition threw (a race — a concurrent status change between validation and execution). Engine reuse over whole-upload atomicity is **intentional** — bypassing the engine to wrap N rows in one transaction would duplicate the orchestrator + lose its guards/audit. If whole-upload atomicity is ever required, it must come via the engine accepting an external transaction, not via bulk operations bypassing the engine.

> **Partial state is covered by types and code paths, not a dedicated test (Prompt 14).** Triggering the `partial` branch deterministically requires injecting a concurrent status change between validation and per-row execution. The 3-state response is type-checked and the `partial` branch is exercised by the action's branching logic; a concurrent-injection test is not provided.

> **Cap query batching is invariant-by-code-review, not test-asserted (Prompt 14).** `accumulateCapErrors` seeds all capped-faculty counts with a single `findMany` (no per-pair query). This single-query property is guaranteed by the code shape, not by a test — the function is small enough that review catches a regression. If batching ever regresses (someone reintroduces a per-pair query and large uploads slow down), instrument with `prisma.$on('query')` and add an explicit query-count assertion.

### Multi-role bulk operations (Prompt 15)

Bulk review actions for PC / SME / HOG (`/{pc,sme,hog}/bulk-review`) share **one** helper (`lib/bulk-review.ts`, `bulkReview(config, csv, actor)`) parameterised by a `BulkReviewRoleConfig` ({ `queueStatus`, `allowedActions`, `commentRequiredFor`, `eventMap`, `auditAction`, `authorityCheck?`, `executeRow` }). Each role's `config.ts` is plain data + functions; its `actions.ts` is a thin `'use server'` auth wrapper; its `page.tsx` reuses the role-agnostic `<BulkReviewForm action={…} />` (the bound server action is passed as a concretely-typed prop). Per-role authority and effects are **config-driven, not branched** inside the helper.

- **The bulk path mirrors single-action validation — a bulk upload is never a rule-bypass channel.** Comment-required (`pc_rework` / `sme_revert` / `hog_reject` / `hog_rework`), the SME per-request authority (`SmeAssignment.smeUserId === actor.id`, mirroring `assertAssignedSme`), and the action→event map all match what the single-action server enforces.
- **`executeRow` asymmetry is honest:** PC/HOG inline the `transition()` + `Approval` write (their single-action effects aren't extracted); SME reuses the `smeApprove`/`smeRevert` helpers. **`sme_revert` is the only action that writes BOTH an `Approval(SME_REVIEW, REWORK)` row AND a `Comment`-thread row** (the faculty revert-banner feed) — every other action writes only `Approval.comments`. The SME integration test asserts both rows exist so a refactor can't silently drop the Comment write.

> **Defense-in-depth status checks (Prompt 15).** Bulk operations add a pre-flight status check (`request.status === queueStatus` → `request_not_in_queue`) that single-action paths don't have. This is intentional — bulk's atomic-reject semantics need to surface status errors during _validation_, not during transition. Single-action paths can rely on the engine's `nextStatus` guard because they fail one at a time. Both layers (bulk pre-check + engine guard) provide defense-in-depth. Future contributors must **not** "harmonize" by removing the bulk pre-check.

> **The `*.bulk` audit-marker pattern (Prompts 13/14/15).** Bulk operations write two kinds of audit entry per row: (1) the engine's per-transition audit (`handout.transition.EVENT_NAME`) — the authoritative record, atomic with the workflow state change; (2) a `*.bulk` marker (`request.create.bulk`, `request.allocate.bulk`, `pc_review.bulk`, `sme_review.bulk`, `hog_review.bulk`) carrying a shared `bulkSessionId` for grouping. The marker lets admin audit queries identify which entries came from one bulk operation. The engine audit is the truth; the marker is the breadcrumb (best-effort — never fails an already-applied row).

### E2E spec cleanup convention

E2E specs that create **persistent** data must explicitly clean it up in `afterEach`/`afterAll`, inside `try/finally` so a mid-test failure still runs the deletion (and logs if cleanup itself fails — never let a test failure cascade into permanent dev-DB pollution). Pattern: seed sentinel-namespaced data in `beforeEach`, reference it through the spec, delete it after. Dev-DB accumulation across PRs is real (28 requests / 13 SmeAssignments by Prompt 13, from earlier specs that created via the UI flow without cleanup); preventing it per-spec is cheaper than periodic `migrate reset`. Rows with system-generated natural keys (a bulk-created `HandoutRequest` gets a real `HMP-YYYY` refNo, not a sentinel one) are cleaned via their sentinel **parent** (the `courseOfferingId` of the spec's own offering), not a refNo prefix.

> **Convention validated immediately by a self-introduced violation in the same PR (Prompt 13).** Right after this block was written, the Prompt 13 integration test was found creating `Notification` rows (via the inline notify path) that its `afterEach` didn't clean — the exact pollution the convention warns against. The fix turned the gap into a positive: the `afterEach` now both deletes those rows AND the test asserts the notification side-effect happened (the Risk 7 lesson). Re-reading a freshly-written convention while you work is what catches the next violation.

> **Integration tests must scope assertions to sentinel data, not global state (discovered in Prompt 15).** A test that asserts "X count didn't change" against a whole table races concurrent test _files_ (vitest runs files in parallel) that touch the same table — a latent flake that passes until enough concurrent DB tests exist to trip it. Scope to the spec's own sentinel rows: `count({ where: { courseOfferingId: sentinelOffering.id } })`, not a bare `count()`. Prompt 13's `bulk-create` atomic test used a global count and slept until Prompt 15's concurrent integration files exposed it; it was rescoped to match the pattern Prompt 14 already used. Same lesson as Risk 6 — a higher layer (the full parallel suite) catches what a single-file run passes.

> **E2E specs without cleanup-by-fixture leave orphans in the dev DB (named offender: `m4-allocate-assign`).** Specs from the Prompt 4 era predate the cleanup-by-fixture convention (Prompt 13) and create real (non-sentinel) `HandoutRequest` rows via the UI flow with no teardown — `m4-allocate-assign` leaves an `ASSIGNED` `HMP-YYYY-####` request behind on every run (`HMP-2026-0002` was caught + cleaned during the Prompt 23-a Path B walkthrough). Operational mitigation until retrofitted: a pre-walkthrough orphan check (`handoutRequest` rows on the target course/offering) + manual cascade-delete (`prisma.handoutRequest.delete` cascades all children except `Notification`, which is keyed by `meta.requestId` JSON and must be deleted separately). If a "retrofit older E2E specs with cleanup-by-fixture" sweep ever happens, `m4-allocate-assign` is the named target.

> **Playwright `clearCookies()` role-switching produces benign "Failed to fetch RSC payload" console errors.** Switching roles mid-spec by clearing cookies invalidates the session while the Next.js router still has in-flight prefetches for role-sidebar links → each logs `Failed to fetch RSC payload … Falling back to browser navigation. TypeError: Failed to fetch`. The fallback navigation works, so tests still pass. The Prompt 23-a Path B walkthrough captured **17** of these — all this one artifact, NOT a product regression (no CSP violations, no hydration errors, no action failures). Real users clicking "Sign out" don't hit the race the same way. Don't waste time debugging this pattern; it's the test harness, not the app.

### Handout attachments (Prompt 16)

> **Attachments are request-scoped and mutable across versions.** The `Attachment` model hangs off `HandoutRequest`, not `HandoutVersion` — supplementary files (exam papers, slides, references) are supporting material for the whole request and deliberately persist as the handout is revised; they are NOT versioned snapshots. Upload/delete are gated by request _status_ (editable while IN_PROGRESS / REWORK_REQUESTED; locked once APPROVED / PUBLISHED / ARCHIVED) and by uploader identity (only the uploader deletes their own file), not by which version is current. Don't reach for a per-version attachment model "for symmetry" — that's a different feature (per-version evidence), and the request-scoped semantics here are intentional. See [lib/attachments.ts](../apps/web/src/lib/attachments.ts), [api/handouts/[requestId]/attachments/route.ts](../apps/web/src/app/api/handouts/%5BrequestId%5D/attachments/route.ts), [(shared)/attachment-actions.ts](<../apps/web/src/app/(shared)/attachment-actions.ts>).

> **Workflow transitions never block on external services.** A `transition()` must not be rolled back — or even delayed — by S3 (or any external) availability. The archive cold-storage tagging ([`tagAttachmentsArchived`](../apps/web/src/lib/attachments.ts)) runs **POST-commit, outside the transition transaction**, is fully best-effort (outer try/catch around the listing query, inner per-object try/catch, `console.warn` on every failure, never throws). An untagged object simply isn't moved to Glacier — harmless. The same principle already governs `notifyTransition` (§"How notifications are fired"). Conversely, a **user-initiated** delete ([`deleteAttachmentAction`](<../apps/web/src/app/(shared)/attachment-actions.ts>)) is fail-_visible_: if S3 is unreachable it keeps the DB row and surfaces a retry error rather than orphaning the object or lying that it deleted — the asymmetry is deliberate (a background lifecycle nicety swallows failures; a direct user action reports them).

> **S3 keys for user-uploaded content are opaque UUIDs, never user-provided filenames.** Attachment objects are stored at `attachments/<requestId>/<crypto.randomUUID()>` ([route.ts](../apps/web/src/app/api/handouts/%5BrequestId%5D/attachments/route.ts)); the original filename is persisted only in the `Attachment.filename` DB column for display. This avoids path traversal, key collisions, and encoding pitfalls from untrusted filenames, and decouples the durable key from a name the user might want to change. Downloads are always via short-lived **presigned GET URLs** minted on demand from the durable `s3Key` ([`getPresignedDownloadUrl`](../packages/integrations/src/storage.ts)) — never persisted (a stored URL goes stale / leaks) and never public objects. Server-side type + size validation ([`validateAttachment`](../apps/web/src/lib/attachment-validation.ts)) is authoritative; the client check is courtesy-only.

### AI cost tracking (Prompt 17)

> **The AI cost ledger records at the central chokepoint; cache/stub hits are free.** Every AI call funnels through `getAiClient()` → `chatJson`/`embedText` ([packages/ai/src/client.ts](../packages/ai/src/client.ts)), which return `{ tokens, model }`. The feature functions ([quality.ts](../packages/ai/src/quality.ts), [handout-generator.ts](../packages/ai/src/handout-generator.ts), [structured-handout-generator.ts](../packages/ai/src/structured-handout-generator.ts), [embeddings.ts](../packages/ai/src/embeddings.ts)) call [`recordAiUsage`](../packages/ai/src/usage.ts) immediately after the REAL provider call — never on the cache-hit or stub (`noop`) paths, because those make no API call and incur no cost. Recording is **best-effort**: the whole body is wrapped so a tracking-infra failure can't break the user-facing AI feature (same discipline as Prompt 16 archive tagging + the notifications module). Chat operations record on both success and failure (the failure row carries `succeeded=false` + `errorMessage`); embedding operations record on success only (failures self-heal into the recommender's heuristic fallback). Attribution context (`{ actorId?, handoutId? }`) is threaded as an optional last param into each feature fn — additive, behavior-preserving: existing callers that omit it get anonymous rows.

> **Embedding costs are labeled by the _triggering operation_, not the technical call.** `FACULTY_RECOMMENDATION` includes the embedding calls a recommendation makes; `EMBEDDING` covers admin corpus refresh. Same underlying `embedText` call, two different cost categories. This makes the dashboard's by-operation breakdown reflect what stakeholders care about (which features cost what), not what the code did (all calls were `embedText`). The operation label is supplied by the caller via `EmbedUsage` ([embeddings.ts](../packages/ai/src/embeddings.ts)); `recommendFaculty` passes `FACULTY_RECOMMENDATION`, `ensureCorpusEmbeddings` passes `EMBEDDING`.

> **AI model pricing in [`pricing.ts`](../packages/ai/src/pricing.ts) is a snapshot — the canonical source for cost calculation.** It carries a "Verified YYYY-MM-DD" comment. When Anthropic/OpenAI change pricing, update the value AND the date. Past `AiUsageLog` rows use the prices current when the calls happened — **never recalculate historical costs against updated prices. Cost-recorded-at-call-time IS the cost for that call.** This prevents future "recompute historical AI costs at new rates" panic moves. (Verified 2026-06-11: Haiku 4.5 $1/$5 per M, gpt-4o-mini $0.15/$0.60 per M, text-embedding-3-small $0.02 per M — all current.)

> **`AiUsageLog` uses `onDelete: SetNull` for both user and handout FKs.** Cost ledgers must survive deletion of the entities they reference — historical cost data is non-discardable. The dashboard treats a null user as "system / background" (worker + page-render paths have no actor) and a null handout as "not handout-scoped" (recommendations, corpus embeddings). Pattern applies to any future financial/audit table — contrast with the AI _output_ models (`AIDraftLog`/`AIQualityReport`/`AIRecommendation`) which `Cascade` because they're disposable handout artifacts, not a financial record. (Checked Prompt 16's `Attachment.uploaderId`: it's `Restrict`, not `Cascade` — a user with attachments can't be hard-deleted; the app soft-deletes via `User.active` anyway. Different shape, not a vaporization risk.)

> **Budget enforcement is a soft cap (Prompt 17 default).** `AI_MONTHLY_BUDGET_USD` (default $200): when month-to-date spend exceeds it, [`maybeWarnBudget`](../packages/ai/src/usage.ts) writes a single in-portal `Notification` to every admin — deduped to once per month via `Notification.meta` (`kind: 'ai.budget.exceeded'` + `month: YYYY-MM`), reusing the SLA-reminder pattern. AI stays enabled (no hard block). The findFirst-then-create dedup has a tiny race window (acceptable — worst case 2 alerts/month). See §6 for the deferred hard-cap + email-delivery items.

> **Env-var guards that filter falsy values treat unset, empty, and zero identically.** `monthlyBudgetUsd()` uses `Number.isFinite(raw) && raw > 0 ? raw : 200` — so `AI_MONTHLY_BUDGET_USD=0` is read as "unset" and falls back to $200, NOT "zero budget / alert on any spend." This is **intentional** defensive behavior (an emptied/typo'd env var yields the sane default, not a flood of alerts), and it surfaced as a self-caught test bug (the budget integration test first set `'0'`, expected a fire, got the $200 default → fixed to a tiny positive budget). If "explicit zero" ever needs to differ from "unset" (e.g. an env-driven way to disable AI), use an explicit null check (`raw === '' ? default : Number(raw)`) or a separate `AI_BUDGET_ALERTS_ENABLED` flag — don't reuse a truthiness guard. General rule: a `raw > 0` / `if (env)` guard cannot distinguish those three states; reach for explicit checks when the distinction matters.

### Choosing `onDelete` for a new foreign key

> **FK `onDelete` behavior reflects what historical question matters — decide it explicitly per FK, never pick the easiest default.** This repo now has all three in deliberate use:
>
> - **`SetNull`** — when the related record's _value_ matters regardless of who/what owned it. Financial/audit records: [`AiUsageLog.userId`/`handoutId`](../packages/db/prisma/schema.prisma) (Prompt 17) — keep the cost, null the reference; historical aggregate sums stay accurate.
> - **`Restrict`** — when the relationship is _structurally required_ and soft-delete is the proper pattern. Content ownership: [`Attachment.uploaderId`](../packages/db/prisma/schema.prisma) (Prompt 16) — every attachment must have an uploader, so a user with attachments can't be hard-deleted; the app soft-deletes via `User.active` instead (see "User soft-delete" below).
> - **`Cascade`** — when the related record has _no meaning without its parent_. Transient artifacts: `HandoutVersion`/`AIDraftLog`/`AIQualityReport` when their `HandoutRequest`/`Handout` is gone.
>   The selection follows the question — "how much did we spend?" (→ SetNull, survive deletion), "who owns this file?" (→ Restrict, block deletion), "does this row mean anything alone?" (→ Cascade). This is the schema-design reference for Prompts 18/21 and any future tables.

### User soft-delete (Prompt 18)

> **Soft-delete / disable is via `User.active` — there is NO `deletedAt`.** A user is disabled by setting `active = false` ([`toggleUserActiveAction`](../apps/web/src/app/admin/users/actions.ts), the admin UI). This is the single, fully-wired mechanism: **auth rejects inactive users** ([config.ts:35](../packages/auth/src/config.ts) — `if (!user.active) return null`), and every roster / picker / recipient query filters them out. The canonical filter is [`ACTIVE_USER_FILTER`](../packages/db/src/user-helpers.ts) (`{ active: true }`) — spread it into the `where` of any `User` query that must exclude disabled accounts; the action-layer guard [`requireActiveUser(id)`](../packages/db/src/user-helpers.ts) loads-or-throws. (A scaffolded `User.deletedAt` column existed but was **never read or written** — a dead second soft-delete signal. Prompt 18 dropped it; wiring it up would have created a two-source-of-truth bug. See "audit doc vs survey" below — the audit's own §7-C flag implied "wire it up," which the Phase 1 survey corrected to "drop it.")

> **Single-action and bulk paths must filter on the same field.** When a single-action path filters a field (e.g. `active=true` in the HOG faculty/SME pickers), the corresponding bulk-path's per-row resolution must filter the same field. Prompt 14's `bulk-allocate` resolved faculty/SME by email checking only the **role**, not **active** — so a deactivated user excluded from the single-action dropdown could still be bulk-allocated via CSV. Prompt 18 closed this (`faculty_inactive` / `sme_inactive` per-row rejections). The `ACTIVE_USER_FILTER` constant makes the rule enforceable by convention. **Future bulk operations: verify every per-row resolution point filters the same fields as its single-action equivalent.**

> **Active-filter sites deliberately NOT converted to `ACTIVE_USER_FILTER` (Prompt 18), with reasoning** — documents the negative space so future contributors don't assume "every active filter must use the constant":
>
> - **Auth** ([config.ts:35](../packages/auth/src/config.ts)) — post-fetch `!user.active` check (load-by-email then verify), a different idiom from filter-at-query. Compatible behavior; different shape.
> - **Admin user list** ([admin/users/page.tsx](../apps/web/src/app/admin/users/page.tsx)) — intentionally lists ALL users so an admin can toggle them.
> - **Historical displays** (audit log, AI-metrics top-user/handout name resolution) — disabled/removed users must STILL render their names; filtering would blank historical references.
> - **`@hmp/ai` internals** (recommender/embeddings/usage) — already correct; a cross-package import for a two-word constant is churn with no leak-surface payoff.
>   Future refactors touching these sites should consider whether to convert — but don't mechanically convert without reason. Also note: `FacultyAssignment.active` is a DIFFERENT field (is the assignment current) — `ACTIVE_USER_FILTER` is for `User` queries only.

> **Audit doc entries vs Phase 1 survey: the survey wins on facts.** Audit entries describe symptoms (dead column, missing filter) but were written at a point in time and may lack the complete picture. When a Phase 1 survey contradicts an audit entry, the survey is closer to truth (it reads current code). The §7-C / §8-item-6 `deletedAt` entry was accurate about the symptom (dead column) but its implied remediation ("complete the soft-delete pattern" → wire it up) was wrong — it didn't account for `active` already doing the work. Audit entries are signals for investigation, **not authoritative remediation plans**. Phase 1 survey always overrides audit doc on factual matters.

> **Verify what's actually in the message before responding — symmetrically.** When responding to a phase report, a pasted artifact, or a referenced document, verify it is actually present before acting on it. **Planner-side confabulation incidents: five recorded.** (1) ≈PR #22 era — a SAML analysis from a _different_ project treated as authoritative for this codebase. (2) ≈pre-PR #29 — phantom IdP metadata XML responded to as if pasted (no XML was sent). (3) Between Prompt 18 merge and the Prompt 20 push gate — a fabricated "doubts conversation" (role emails, Google Drive, bulk accuracy) + workflow-clarification + corpus-count request, none of which occurred. (4) Between Prompt 21 Phase 2 approval and the Phase 3 report — a fabricated "Docker/MinIO question" **attributed to the executor** (words/intent the executor never expressed). (5) Prompt 23-a framing — **three distinct factual errors stacked in one message**: wrong PR attribution ("Prompts 19-21" for corpus work that was actually Prompts 11f-a/b1/b2), a propagated earlier-conversation false claim ("Prompt 11f was dry-run/analysis only" when it is a real upserting end-to-end pipeline), and a fabricated corpus directory path that did not exist on disk — all three caught by Phase 1 reading the actual code + listing the actual filesystem. Each was caught only because the other party verified actual conversation content and **refused to play along** (e.g. declining to answer a question never asked). **Frequency: five in ~7 PRs is a sustained pattern, not noise.** Incident #4 is qualitatively worse — it misattributed words to a person rather than fabricating ambient content. Incident #5 introduces a new shape — **multiple confabulations stacked in one message, each reinforcing the others' plausibility** (the wrong PR attribution made the false dry-run claim seem grounded; the dry-run claim made the fabricated path seem natural). The defense: a single push-back may need to correct several errors at once, not just the most obvious one. **Crucially: the planner cannot independently detect these — its context may show fabricated content as if real; the executor's verification against the actual transcript is the only reliable check.** The fix runs both directions: the executor verifies pasted evidence against the real codebase before pivoting; the reviewer grounds every response in actual conversation content, not plausibility (when in doubt, quote the counterpart's actual recent message rather than infer). If a counterpart references something that isn't there, correct the record rather than build on the phantom — even when accommodation would feel like normal helpful work.

> **Premise correction as a recurring pattern (4 occurrences to date).** A sibling discipline to the confabulation defense above: prompts repeatedly frame work as "build new infrastructure for X" when X-infrastructure already exists. (1) **Prompt 17** — AI cost-tracking infrastructure mostly existed; the prompt assumed greenfield. (2) **Prompt 18** — `User.deletedAt` was a dead column; the real soft-delete mechanism was `active`. (3) **Prompt 22** — the PC allocation gate already existed (`confirmAssignmentAction`); only a reject edge was missing. (4) **Prompt 23-a** — the entire corpus-import pipeline (parser + idempotent upsert + admin approval UI + bulk-approve + auto-fetch Tier 2) was already built across Prompts 11f-a/b1/b2; only the operational _run_ remained, so the "PR" collapsed to a docs commit + running the existing CLI. Across these four, **~30–90% of each prompt's stated scope reduced to "complete a small missing piece" — and they collectively avoided ~1500 LOC of duplicate-or-conflicting code.** The discipline: **Phase 1 reads the existing code FIRST and proposes scope against reality, not against the prompt's assumed-greenfield framing. Survey findings override the prompt's premise on factual matters.** This is the same root defense as the confabulation entry — verify against the actual codebase, not against a narrative's coherence.

### Security headers + rate limiting (Prompt 20)

> **Edge-runtime constraint for middleware.** Any package imported by [middleware.ts](../apps/web/src/middleware.ts) must be edge-compatible. `ioredis` is Node-only and cannot enter the middleware module graph. Rate limiting therefore lives in `apps/web` at **Node-runtime endpoints** (the auth-route wrapper, Route Handlers, server actions), **not** in middleware. Middleware handles **CSP/nonce generation only** (uses Web-Crypto `crypto.randomUUID()`, edge-safe). This prevents future "centralize rate-limiting in middleware" refactors that would break the edge build.

> **Two Redis clients, intentionally different configs.** BullMQ ([@hmp/queue connection.ts](../packages/queue/src/connection.ts)): `maxRetriesPerRequest: null` — commands queue forever, correct for background jobs. Rate-limit client ([lib/rate-limit.ts](../apps/web/src/lib/rate-limit.ts)): `maxRetriesPerRequest: 1, enableOfflineQueue: false, lazyConnect: true, connectTimeout: 1000` — **fast-fail**, required so a Redis outage doesn't _hang_ a user-facing request. The rate-limit client config is commented at its declaration explaining why it differs.

> **Rate limiting fails OPEN on Redis unavailability.** On any Redis error (or unconfigured `REDIS_URL`): allow the request, write a `ratelimit.unavailable` audit row, `console.warn` for ops visibility. Rationale: total auth lockout during a Redis outage is worse than a brief brute-force window during the same outage. Monitorable via audit-log frequency. The audit write has its **own inner try/catch** so an audit/DB failure during a Redis outage can't propagate up and break the request (same best-effort discipline as Prompt 16 archive tagging).

> **Rate-limit response shape varies by endpoint type.** HTTP Route Handlers + the auth route return **429 with `Retry-After`** (HTTP semantics — `tooManyRequests()`). Server actions (AI draft/quality) return **`{ error: 'Rate limit reached — try again in a little while.' }`** matching the existing error-result convention (RPC semantics — they have no HTTP status to set; the dialogs already render `{ error }`). Both are correct for their context. Per-endpoint limits live in one `RATE_LIMITS` map: **login 5 / 15 min per IP**, **upload 10 / hour per user**, **AI 20 / hour per user**. **Login-throttle placement (caught in Phase 3 verification):** the login form posts via a **server action** (`signInAction`), so the browser never POSTs to `/api/auth/callback/credentials` — the throttle's PRIMARY home is inside `signInAction` (keyed `login:${ip}` via `x-forwarded-for`). The auth-route wrapper (scoped to `/callback/credentials`, bypassing session refresh + future SAML callbacks) is kept as **defense-in-depth** for a direct API POST and shares the same `login:${ip}` counter. The "missing `login:` audit rows after a form login" was the signal that the wrapper alone was the wrong interception point.

> **CSP: nonce `script-src` + `unsafe-inline` `style-src`** ([middleware.ts](../apps/web/src/middleware.ts)). Evidence-based: Next 14 injects inline hydration scripts → nonce (Next propagates the middleware-set nonce to its own scripts; viable because the app is fully dynamic behind auth, so no static-optimization is lost); hand-written `<style>` blocks (login) + ProseMirror runtime inline styles → `unsafe-inline` for styles only (a far weaker vector than script injection). The only external browser origins are Google Fonts (`fonts.googleapis.com` style + `fonts.gstatic.com` font); AI APIs are server-side, S3 downloads are navigations — neither is browser-loaded. **`'strict-dynamic'` has known version-sensitivity with Next 14's nonce'd scripts — Phase 3 verified `'self' 'nonce-…' 'strict-dynamic'` WORKS in the production build** (zero CSP violations across authed pages in the E2E console-error spec; Next 14 propagated the middleware-set nonce to its own inline scripts, so `layout.tsx` needed no change). If a future Next upgrade blocks Next's scripts despite the nonce, the documented fallback is to drop `'strict-dynamic'` and rely on `'self' 'nonce-…'` alone. Both variants are noted at the middleware code site.

> **CSRF.** NextAuth handles auth-endpoint CSRF; App-Router server actions have built-in origin/action-id protection; the session cookie is SameSite=Lax (cross-site POSTs don't carry it). The one explicit addition: an **Origin/Host check** on the attachments Route Handler ([attachments/route.ts](../apps/web/src/app/api/handouts/%5BrequestId%5D/attachments/route.ts)) — reject when an `Origin` header is present and its host ≠ the request `Host`. Lightweight defense-in-depth, no token machinery for a single authenticated handler.

### Reconciliation + operational readiness (Prompt 21)

> **Scheduled jobs use external cron → authenticated HTTP endpoint, not BullMQ repeatable jobs.** `/api/cron/*` routes (`reminders`, `reconcile`) with bearer auth via `CRON_SECRET`, `runtime='nodejs'`, triggered by the platform scheduler. BullMQ is fire-and-forget enqueue only (notifications, AI) — it has no repeatable/cron scheduler in this codebase. New scheduled tasks follow the cron-route pattern (copy [reminders/route.ts](../apps/web/src/app/api/cron/reminders/route.ts)).

> **Reconciliation detection should be an indexable DB query, not external-service polling.** The [`Attachment.archivedTaggedAt`](../packages/db/prisma/schema.prisma) timestamp pattern: the happy-path side effect sets it on success; detection queries `archivedTaggedAt IS NULL AND request.status = ARCHIVED` (the status index is the selective dimension). Cheaper than re-checking external state (S3 tags) every sweep, and more reliable than audit-log queries. **Pattern for future reconcilable effects: add a timestamp column on the affected entity.** The reconciliation framework lives at [lib/reconciliation.ts](../apps/web/src/lib/reconciliation.ts) — a new effect implements `ReconciliationEffect<T>` (detect → reconcile → describe) and is added to the `EFFECTS` registry; nothing else changes. The sweep is best-effort at every layer (a failing `reconcile` → `reconciliation.failed` audit + continue; a failing `detect` → logged, other effects still run) and rate-limited (per-effect cap + inter-call delay) so a backlog drains over days without hammering S3.

> **When adding reconciliation to a previously-built best-effort effect, the happy-path code must record success.** Otherwise reconciliation can't distinguish "successfully done" from "never attempted" and treats working state as unreconciled forever. Prompt 21 modified Prompt 16's `tagAttachmentsArchived` to set `archivedTaggedAt` on a successful tag — an additive change inside the existing per-object try/catch, recording success only; the failure path is untouched (still swallows + `console.warn` → reconciliation catches it).

> **Reconciliation evaluation criteria — three tests; reconcile only if all pass.** (1) **Detectable** — durable state distinguishing "unreconciled" from "reconciled" must exist. (2) **Reconcilable** — the effect must be retried idempotently with high confidence. (3) **Worth it** — delayed reconciliation must provide real value (not time-sensitive, self-healing, or regenerable on demand). **Archive tagging** passes all three → reconciled. The three other best-effort effects were evaluated and **deliberately not reconciled**: **budget warning** fails (3) — self-heals on the next cost-incurring AI call that month; **notification email delivery** fails (3) — time-sensitive (a 3-day-late email is noise; the in-portal `Notification` row exists regardless, tracked via `status=FAILED`); **on-submit quality report** fails (3) — advisory, costs AI budget, regenerable on demand. Documenting _why not_ is more valuable than fabricating reconciliation that adds no value.

> **S3 attachments bucket has versioning enabled** ([setup-s3-lifecycle.ts](../packages/integrations/scripts/setup-s3-lifecycle.ts), Prompt 21) for accidental-delete recovery — a deleted/overwritten object is restorable from a prior version (see [disaster-recovery.md](../disaster-recovery.md) Failure class 2). Noncurrent-version expiry is intentionally unset (a retention/cost tuning knob, not a default). Backup posture is verified by [verify-backups.ts](../packages/integrations/scripts/verify-backups.ts) (S3 in code; RDS via the documented `aws rds describe-db-instances` CLI — no `@aws-sdk/client-rds` dep for one deploy-time check).

### PC allocation review — reject path (Prompt 22)

> **Premise-correction pattern (2nd occurrence, after Prompt 18's `deletedAt`).** The prompt proposed inserting a NEW `PC_ALLOCATION_REVIEW` state + queue + confirm action; the Phase 1 survey found the gate **already existed** — `confirmAssignmentAction` (`ALLOCATED → ASSIGNED`, PC-gated) IS the PC allocation confirm, and faculty already can't work until it fires (`FACULTY_STATUSES` excludes `ALLOCATED`). The only real gap was a **reject** edge. Building the proposed new state would have duplicated the gate (double PC confirmation). Pattern: **when a Phase 1 survey finds a feature is ~85% built, complete the missing piece — don't duplicate the existing infrastructure.** ~200 LOC of completion replaced ~600 LOC of duplicate-and-redesign. The discipline: Phase 1 surveys existing code BEFORE planning new infrastructure; survey findings override the prompt's assumed-greenfield framing. PC now has **two review surfaces**: allocation (confirm `ASSIGNED` / reject `ALLOCATION_REJECTED`, at `ALLOCATED`) and content (`REVIEW_APPROVED`/`REVIEW_REWORK`, at `SUBMITTED`).

> **Hard-delete vs soft-deactivate is driven by schema reality, not preference.** PC allocation reject ([rejectAllocationEffect](../apps/web/src/lib/pc-allocation-review.ts)) clears HOG's allocation two different ways: `FacultyAssignment` → **soft-deactivate** (`active=false`; it has the field + no unique constraint, and the per-semester cap count filters `active=true`); `SmeAssignment` → **hard-delete** (no `active` field + `requestId @unique`, so leaving it would block HOG's re-allocate `create` with a unique violation). The asymmetry is a consequence of schema design, not a behavior difference — conceptually both assignments are cleared and HOG re-allocates fresh. **History is preserved in the AuditLog chain regardless:** `FACULTY_ALLOCATED` (meta.smeUserId) → `ALLOCATION_REJECTED` → `FACULTY_ALLOCATED`. The shared effect is used by BOTH the single action and the bulk path so the destructive multi-step can't drift.

> **Notification matched-pair principle.** If a role is notified when a state is entered, it must be notified when that state is exited/retracted. PC allocation reject notifies **HOG** (re-allocation owner) + **faculty** (who WAS notified at `FACULTY_ALLOCATED` — `handout.allocated`). SME is **not** notified — it wasn't notified at allocation (it hears at `SME_REVIEW_REQUESTED` later). Implementation gotcha worth remembering: the reject soft-deactivates faculty assignments _inside_ the transaction, so the post-commit `notifyTransition` recipient query for `ALLOCATION_REJECTED` must read **all** assignments (not just `active=true`) or the just-rejected faculty would be missed.

> **Deferred candidate: `/pc/allocation-review` vs `/pc/content-review` UI split.** PC's `/pc/requests` queue currently lists allocation-review (`ALLOCATED`) and content-review (`SUBMITTED`) rows in one status-filtered table, distinguished by the status badge + filter dropdown. Prompt 22 added a bulk `/pc/allocation-review` page + clear Confirm/Reject buttons on the per-request panel, but did NOT split the main queue (scope discipline). If PC users find the combined queue confusing in real use, a follow-up can split it. Not blocking.

> **UI text changes are stealth breakers for accessible-name E2E selectors.** Renaming user-facing text (button labels, heading text, link text) silently breaks every E2E spec that selects by accessible name (`getByRole('button', { name: /text/i })`). Before renaming such text: grep the E2E suite for the old text AND check the full rename surface for missed callsites. The "Confirm assignment" → "Confirm allocation" rename (Prompt 22) broke **four** specs (`m6c-comments`, `m8-ai-layer`, `m4-allocate-assign`, `m10-structured-editor`) — the scope was wider than the diff suggested, because handouts are advanced through the PC confirm gate by name-based clicks all over the suite. Pre-rename grep is cheaper than post-rename debug.

> **E2E assertions against `revalidatePath` server actions must target the persistent post-condition, not transient UI feedback.** A server action that calls `revalidatePath` re-renders the page; on a `force-dynamic` page whose panel is gated on status (e.g. the assignment panel renders only when `ALLOCATED`), a successful transition **unmounts the panel and destroys its transient success message** before a Playwright assertion can race it. Prompt 22's reject E2E originally asserted the `assignment-success` line and flaked for exactly this reason — the reject succeeded (audit row written) but the success `<p>` was gone. Fix: assert what is true **after** revalidation finishes — element detachment (`getByTestId('reject-allocation-btn').toHaveCount(0)`), a changed status badge, or an audit-log row — never the ephemeral feedback. Note the asymmetry: **integration tests pass against the transient state because they bypass revalidation; E2E specs against the live server cannot.** If an integration test is green but the E2E flakes on a success message, suspect this.

### Word + PDF export (Prompt 23-b)

> **The docx builder mirrors `renderBitsHandout`'s section walk — one section-mapping, two output formats.** [build-docx.ts](../apps/web/src/lib/export/build-docx.ts) emits a print-faithful Word document (Arial, A4, 1″ margins, multi-campus BITS banner in the page header) by walking the SAME `BitsHandoutV1` sections, in the same order, as the web HTML renderer ([handout-renderer.ts](../packages/db/src/handout-renderer.ts)). When a handout section is added/removed, both walkers must change together (there's no shared abstraction — they're parallel renderers for two media). The route is `GET /api/handouts/[requestId]/export/[format]` (`docx|pdf`) — a Route Handler, not a server action, for binary streaming + `Content-Disposition`. Filename convention: `HMP-<refNo>-<courseCode>-v<versionNo>.<ext>` (unique · readable · versioned; non-`[A-Za-z0-9._-]` → `_`). Export requires structured `data` — legacy `data:null` handouts 404 (`no_structured_handout`).

> **Filename/string composition from pre-formatted server identifiers must be asserted end-to-end, not just at construction (Prompt 23-b).** The export filename first came out `HMP-HMP-8888-…` — the template was `HMP-${refNo}-…` but `refNo` already carries the `HMP-YYYY-####` prefix. A unit test of the construction would have **passed** (the function did what it was coded to do); the **E2E** caught it by asserting the actual `Content-Disposition` header against the documented format. General principle: when a function composes a user-facing string from server-side identifiers, the "does this match the documented format" check must run end-to-end — a unit-tested function can produce well-formed output that's still wrong because the input contract wasn't what the function assumed.

> **HTML→docx is hand-rolled over the 13-tag TipTap allowlist — not a library.** [html-to-docx.ts](../apps/web/src/lib/export/html-to-docx.ts) converts the rich-text fields. An empirical survey of all 294 structured corpus imports found the 6 HTML fields contain **only `<p>`** (588 occurrences, zero other tags); the other 12 allowlist tags (`br, strong, b, em, i, u, ul, ol, li, a, span, code`) exist for faculty-authored editor content. The subset is small, bounded, and known — `html-to-docx` would be a dependency for what's empirically paragraphs. Decisions baked in: nested inline formatting accumulates; `<ul>`→docx bullets, `<ol>`→manual "N. " prefix (no Document numbering-config coupling); `<a>` honours only http/https/mailto; empty input → no paragraphs.

> **The export gate is capability-based, role × status × ownership (the 1F matrix).** Pure function [access.ts](../apps/web/src/lib/export/access.ts) `canExportHandout`: IC/HOG/Admin → APPROVED|PUBLISHED; faculty → their OWN handout (active assignment) at SME_REVIEW|SUBMITTED|UNDER_REVIEW|REWORK_REQUESTED|APPROVED|PUBLISHED ("has been submitted at least once"); PC/SME → never (no privileged role, no assignment). It's by-capability not by-exclusion: a PC who is also the assigned faculty CAN export via the faculty path. The route re-checks regardless of UI (defence in depth). Exhaustively unit-tested (93 cells).

> **No admin handout-detail page exists — admin export is route-level only (Prompt 23-b finding).** The planned 4th UI surface ("admin handout view") isn't a real page: `/admin/*` is dashboards (users, roles, audit, corpus-imports, ai-metrics, queues…), with no per-handout detail view. The download menu ships on the 3 real surfaces (faculty assignment, IC request, HOG request). Admin retains route-level export access (the gate grants ADMIN) but has no button to click — acceptable; building an admin handout-detail page is its own scope, **deferred**. The discoverability gap: until that page exists, an admin must know the URL pattern (`/api/handouts/<id>/export/<docx|pdf>`) to export; the UI affordance arrives with the admin handout-detail page.

> **Logo-variant finding: the repo's `bits-logo.png` was the wrong asset for export.** `apps/web/public/bits-logo.png` is the crest **emblem** only (single "Pilani"). Real BITS WILP handout headers (`word/media/image1.png` in any corpus .docx) carry the full **multi-campus banner** — crest + "BITS Pilani" + "Pilani | Dubai | Goa | Hyderabad". The export uses the extracted banner committed at [apps/web/src/lib/export/assets/bits-header.png](../apps/web/src/lib/export/assets/bits-header.png) (read server-side via `fs`, NOT a public URL — it's embedded in docs, never browser-served). The in-app `public/bits-logo.png` was left untouched.

> **LibreOffice headless concurrency: spawn with `-env:UserInstallation=file:///…/lo-<unique>` per invocation.** [docx-to-pdf.ts](../apps/web/src/lib/export/docx-to-pdf.ts) gives every conversion its own temp profile dir (under a per-call `mkdtemp`). Without this, concurrent conversions collide on LibreOffice's default profile lock and hang. The 30s timeout + SIGKILL covers the failure mode where this guard fails or the conversion itself stalls. Temp files (input docx, output pdf, profile) are cleaned in `finally`. `SOFFICE_BIN` env (default `soffice`); ENOENT → typed `PdfConversionError{kind:'missing-binary'}` → route 503 (local dev / no LibreOffice, like the MinIO specs), else 500. PDF is infra-gated and probe-skips locally; Word export works anywhere. **Pattern applies to any future subprocess-based document conversion.**

### Authoring migrations in this environment

> **Hand-author the SQL, then apply via `prisma migrate deploy` — `prisma migrate dev` blocks on interactive prompts here.** On some environments (observed on Windows/PowerShell, Prompt 16) `prisma migrate dev` (and `--create-only`) abort with "non-interactive environment not supported" whenever the change needs a confirmation prompt (e.g. adding a required column or a `@unique`). The workaround: hand-author `packages/db/prisma/migrations/<timestamp>_<name>/migration.sql` matching Prisma's SQL + naming conventions, then run `pnpm --filter @hmp/db exec prisma migrate deploy` (non-interactive, applies all unapplied migrations idempotently) followed by `prisma generate`. Verify no drift afterward — a clean deploy reports "Database schema is up to date!". Production already uses `migrate deploy` (see deployment-runbook "Migration lifecycle"), so a hand-authored migration is applied the same way in dev and prod.

---

## 2. Naming patterns

### File layout

- App routes nested by role: `apps/web/src/app/{admin,ic,hog,pc,faculty}/...`. Public/auth routes at root level (`login/`, `notifications/`).
- One server-action file per route folder: `actions.ts`. Sometimes split — faculty has both `actions.ts` (workflow) and `ai-actions.ts` (AI-only mutations).
- Co-located components stay in the route folder (`request-form.tsx`, `accept-panel.tsx`, `editor-panel.tsx`). Cross-route components live in `apps/web/src/components/`.
- Domain helpers in `apps/web/src/lib/` (audit, sla, notifications, faculty-load, faculty-signals, handout-versioning, routing, tiptap-extensions). Each has a `.test.ts` peer where covered.
- Shared route-group helpers in `apps/web/src/app/(shared)/` — currently `comment-actions.ts`.

### Route group `(shared)`

Next.js parentheses route group used to host **cross-role server actions** (commenting works for IC/HOG/PC/FACULTY). Does not appear in URLs.

### Action naming

- IC: `createRequestAction`, `publishAction`, `archiveAction`.
- HOG: `allocateFacultyAction`, `finalApproveAction`, `finalRejectAction`, `hogRequestReworkAction`, `regenerateRecommendationAction`.
- PC: `confirmAssignmentAction`, `pcReviewApproveAction`, `pcReviewReworkAction`.
- Faculty: `acceptAssignmentAction`, `startEditingAction`, `saveDraftAction`, `submitForReviewAction`, `runQualityCheckAction`.

Pattern: `{actor}{verb}Action` or `{verb}{noun}Action`. Always suffixed `Action`.

### Workflow event naming

`SCREAMING_SNAKE_CASE`, past-tense verb form: `REQUEST_INITIATED`, `FACULTY_ALLOCATED`, `ASSIGNED`, `EDIT_STARTED`, `SUBMITTED`, `REVIEW_REWORK`, `REVIEW_APPROVED`, `FINAL_APPROVED`, `FINAL_REJECTED`, `PUBLISHED`, `ARCHIVED`. Statuses follow the same convention.

### Approval rows

`ApprovalStage` enum: `HOG_REVIEW`, `PC_REVIEW`, `HOG_FINAL`, `IC_PUBLISH`. Decisions: `PENDING | APPROVED | REWORK | REJECTED`. The same stage can be written multiple times across rework cycles — the API does not assume uniqueness.

### Ref numbers

Human-readable `refNo` is `HMP-YYYY-####`, generated by [nextRefNo()](../apps/web/src/app/ic/requests/new/actions.ts) at request-create time. The current implementation is racy under concurrent IC creates (see §6).

---

## 3. Known stubs and where they live

| Component             | File                                                                                                | Behaviour                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Replacement plan                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --------------------- | --------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Taxila LMS publish    | [packages/integrations/src/taxila.ts](../packages/integrations/src/taxila.ts)                       | **No longer a stub (Prompts 9a + 9b).** `publishToLms` is the real two-mode engine and `publishAction` is wired to it; the old stub is deleted. Mode A (HTTP) when `TAXILA_API_URL` is set; Mode B (export ZIP → MinIO → presigned download → IC "Mark as manually published") when it isn't.                                                                                                                                                                                                  | **Operational (when BITS provisions Taxila):** before relying on Mode A in production, run a one-time manual integration test against the real endpoint — the unit test proves the request _shape_ ([taxila.test.ts](../packages/integrations/src/taxila.test.ts) logs the exact body), not that it matches Taxila's live contract. Also verify whether Taxila wants a `semesterCode` (the schema's Semester has only a `name`; we send `semesterName`) — if so, add a `Semester.code` column + remap. |
| SSO provider          | [packages/auth/src/sso.ts](../packages/auth/src/sso.ts)                                             | `StubSsoProvider` throws "SSO not configured" on both `getAuthorizationUrl()` and `exchange()`. `getSsoProvider()` returns it by default.                                                                                                                                                                                                                                                                                                                                                      | Implement real `SsoProvider` (BITS IdP, SAML or OIDC) and call `registerSsoProvider(...)` at boot. The credentials provider in [auth/config.ts](../packages/auth/src/config.ts) has a comment marking where to wire a NextAuth provider that delegates to it.                                                                                                                                                                                                                                          |
| AI client             | [packages/ai/src/client.ts](../packages/ai/src/client.ts)                                           | If `OPENAI_API_KEY` / `ANTHROPIC_API_KEY` is empty, `getAiClient()` returns `createNoopClient(reason)` whose `embedText` / `chatJson` throw `AiUnconfiguredError`.                                                                                                                                                                                                                                                                                                                             | Set the env var. CI runs with `AI_PROVIDER=noop` deliberately.                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| AI handout draft      | [packages/ai/src/handout-generator.ts:239](../packages/ai/src/handout-generator.ts)                 | When client is `noop`, returns `buildStubDraft(course)` — hardcoded 4-module template marked `source: 'stub'`. Persisted as a real `AIDraftLog` row. UI shows an orange "Template stub" banner.                                                                                                                                                                                                                                                                                                | Same — set provider key.                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Workflow `matrixJson` | [packages/db/prisma/schema.prisma:383](../packages/db/prisma/schema.prisma)                         | `WorkflowConfig.matrixJson` is `Json` (no schema). Seeded but no code reads it — admin workflow page edits SLAs only.                                                                                                                                                                                                                                                                                                                                                                          | Decide whether this is dead schema or future approval-matrix overrides.                                                                                                                                                                                                                                                                                                                                                                                                                                |
| AI token-cost columns | [apps/web/src/app/admin/ai-metrics/page.tsx:208-211](../apps/web/src/app/admin/ai-metrics/page.tsx) | The AI-metrics page renders real per-day usage queries (see §7-A — not a stub). The actual gap is that `promptTokens` / `completionTokens` aren't persisted to indexed columns — they live nested in `AIDraftLog.payload` JSON and aren't captured at all on `AIRecommendation` / `AIQualityReport`. The in-page footer admits this verbatim: _"Per-call token counts are not persisted yet — cost rollup will be added once the AI provider returns and we capture token usage per request."_ | Schema migration to promote token columns + wire `client.chatJson` to return + persist token counts. Tracked as §8 item 1.                                                                                                                                                                                                                                                                                                                                                                             |
| BullMQ workers        | README previously claimed "BullMQ + Redis queues" — corrected in this PR                            | **Resolved (Prompt 10).** `@hmp/queue` (BullMQ) + a worker process (`apps/web/src/workers/start.ts`, `pnpm workers`) now exist. Notifications + the on-submit AI quality report route through the queue when `WORKERS_ENABLED=true` (synchronous fallback otherwise / if Redis is down). Opt-in deployment mode.                                                                                                                                                                               | Either wire actual BullMQ jobs (+ a worker process) or remove the queue claim from architecture docs. README rewrite in this PR removes the false claim.                                                                                                                                                                                                                                                                                                                                               |

Nothing in the entire repo contains `TODO`, `FIXME`, `XXX`, or `HACK` markers (verified via repo-wide `rg "TODO|FIXME|XXX|HACK"` across `{apps,packages,infra,docs,scripts}/**/*`). The stubs above are all behind real interfaces and are the only mocked paths.

---

## 4. Test coverage gaps

What's covered (from filenames):

- **Unit (Vitest)** — `packages/workflow/{machine,guards}.test.ts`, `packages/ai/{client,embeddings,schemas}.test.ts`, `packages/integrations/erp.test.ts`, `apps/web/src/lib/{faculty-signals,notifications,sla,routing}.test.ts`.
- **E2E (Playwright)** — login, ic-create-request, m4-allocate-assign, m5-faculty-edit-submit, m6-publish-archive, m7-notifications, m8-ai-layer.

Gaps observed:

- **No transaction-level test for `transition()` rollback.** The off-campus cap re-check inside `allocateFacultyAction`'s `effects` is the most concurrency-sensitive code path in the app and there is no test asserting that concurrent allocations cannot both squeak past the cap.
- **No test for the rework loop.** `SUBMITTED → REWORK_REQUESTED → SUBMITTED` can happen many times; no test asserts version numbering or approval-row accumulation across the loop.
- ~~**No test for `nextRefNo()` race.** Two simultaneous IC creates can produce duplicate `refNo` strings (see §6).~~ **Resolved** — optimistic-retry helper at [ref-no.ts](../apps/web/src/app/ic/requests/new/ref-no.ts); see [ref-no.test.ts](../apps/web/src/app/ic/requests/new/__tests__/ref-no.test.ts) (mocked P2002 retries) and [concurrent.test.ts](../apps/web/src/app/ic/requests/new/__tests__/concurrent.test.ts) (10 concurrent real-DB creates).
- **No test for SSE stream.** `/api/notifications/stream` has no test of reconnection, auth, or the 5-min cutoff.
- **No test for SLA dedup.** `notifySlaReminder` does a complex `prisma.notification.findFirst` over `meta` JSON path queries; no test verifying the dedup window actually suppresses duplicates.
- **No test for `effects` failure rollback.** If a transition's `effects` throws, the status update must roll back. Not asserted in tests audited.
- **Email transport failure.** `sendMail` returns `{ ok: false, error }` and `deliver` marks the notification `FAILED`. No test of that branch.
- **AI cache reuse.** `recommendFaculty` reuses today's row and `runQualityReport` rate-limits within 60s. The reuse-and-bypass paths are not asserted.
- **Permission keys.** `hasPermission` / `requirePermission` exist but I didn't find a test or production caller. Either remove or add a test.

---

## 5. Production-readiness gaps (vs. a deployable university portal)

| Area                                                          | Gap                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Impact                                          |
| ------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- |
| **Auth**                                                      | No real SSO; credentials only. `passwordHash` is bcrypt but there's no password-reset, no lockout, no MFA.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Cannot ship to university IT without SAML/OIDC. |
| **SSL/TLS, headers**                                          | No security-header config (CSP, HSTS, X-Frame-Options) was found in `next.config.mjs`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Default Next.js exposure.                       |
| **Rate limiting**                                             | The AI quality endpoint has a 60s in-DB rate limit; no IP/user rate limiting at the edge or on `/api/notifications/stream`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | DDoS / abuse exposure.                          |
| **CSRF**                                                      | NextAuth handles its own. Server actions are CSRF-safe by Next's design but uploads/cron are bearer-only. The cron route uses `timingSafeEqual` ([reminders/route.ts:15](../apps/web/src/app/api/cron/reminders/route.ts)) — solid.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **File uploads**                                              | `Attachment` model exists with `s3Key`, but no upload route was found in the audited surface. MinIO is wired in compose only. **Unclear — needs human review.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Backups**                                                   | No DB backup story in `infra/`. Compose has volumes; nothing snapshots them.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Observability**                                             | No structured logging (uses `console.error`), no APM, no Sentry. `LOG_LEVEL` env exists, not threaded through.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Migrations**                                                | The repo ships `prisma db push` (`packages/db/package.json:13`) and CI uses it ([ci.yml:70](../.github/workflows/ci.yml)). **No `prisma/migrations/` folder.** Production must use `prisma migrate deploy`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Email**                                                     | Mailhog only. No bounce handling, no template version tracking, no transactional ESP. `sendMail` swallows transport errors into `{ ok: false }`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Notification link URLs are relative**                       | `linkFor()` ([notifications.ts](../apps/web/src/lib/notifications.ts)) returns relative paths (e.g. `/pc/requests/ID`). In-portal notifications resolve these fine (same-origin), but email `<a href>` anchors won't resolve in a real mail client without an absolute base URL. **Known gap — resolve when a production base-URL config lands (Prompt 10 / deployment hardening):** prefix links with an env-configured `APP_BASE_URL`. Surfaced during Prompt 8; SME notification bodies deliberately omit `{{link}}` so no relative URL is rendered as body text in the meantime.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **LMS publish**                                               | **Resolved (Prompts 9a + 9b).** Real two-mode `publishToLms` wired into `publishAction`. Mode A (HTTP, when `TAXILA_API_URL` set) advances straight to PUBLISHED; Mode B (default) exports a ZIP to MinIO, hands the IC a presigned download, and requires an explicit "Mark as manually published" confirmation (prominent action-required callout) before advancing — so the system never claims PUBLISHED without a human confirming the upload. First AWS S3 SDK + MinIO use (`storage.ts`). E2E: m6a (export→confirm) + m6c (ZIP contents + logs + notification), CI-gated on MinIO. **Mode B operational workflow:** IC clicks Publish → system exports ZIP → IC downloads + uploads to Taxila manually → IC clicks "Mark as manually published". The awaiting-confirmation state is visually prominent to prevent forgotten exports.                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Notification queue**                                        | **Resolved (Prompt 10).** BullMQ workers (`@hmp/queue` + `apps/web/src/workers/start.ts`) move notification + on-submit-AI work off the request cycle when `WORKERS_ENABLED=true`; admin observability at `/admin/queues` (counts, failed jobs + retry/delete, TTL'd worker heartbeat with a "may not be running" warning). **Opt-in deployment mode** — production must run the worker process (`pnpm workers`) alongside web; dev defaults to synchronous. Failed-job retry on a `PermanentJobError` (e.g. `AiUnconfiguredError`) fails again immediately rather than retrying forever.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **AI cost controls**                                          | `pricing.ts` exists but `AIDraftLog` doesn't persist token counts in a queryable column — they're nested in `payload` Json. Cost reports require Json-path queries.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Audit completeness**                                        | Login attempts (success/fail) and session events are not audited. RBAC denials (`AuthorizationError`) are not audited.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ~~**Dead column `User.deletedAt`**~~ **RESOLVED (Prompt 18)** | Was defined at schema.prisma:44, never read or written (verified §7-C). The real deactivation path is `User.active`, used consistently. **Prompt 18 dropped the column** (migration `drop_user_deleted_at`, pre-flight-verified 0 rows set) rather than wire up a redundant second soft-delete signal, and canonicalized the `active` filter as `ACTIVE_USER_FILTER`. See §1 "User soft-delete."                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |                                                 |
| **Permission system**                                         | Permissions table seeded but never enforced; only roles are used in gates. Either remove or wire `requirePermission()`. **Partially addressed in Prompt 5**: `handout.advise` permission now seeded AND will be checked by SME route guards in Prompt 6. `hasPermission` + `requirePermission` are now covered by unit tests at [packages/auth/src/rbac.test.ts](../packages/auth/src/rbac.test.ts). The `ADMIN` super-role bypass was made symmetric in `hasPermission` (matched the existing behaviour in `hasRole`). **Candidates for permission-based gates in a future RBAC tightening pass:** `requireRole(RoleName.INSTRUCTION_CELL)` in IC actions (`request.initiate` / `handout.publish` / `handout.archive`); `requireRole(RoleName.HOG)` in HOG actions (`request.allocate` / `handout.review` / `handout.approve`); `requireRole(RoleName.PROGRAMME_COMMITTEE)` in PC actions (`request.assign` / `handout.review`); `requireRole(RoleName.FACULTY)` in Faculty actions (`handout.edit` / `handout.submit` / `handout.read`); admin-area actions (`user.create` / `academic.manage` / `workflow.config` / `audit.read`). Each substitution is one line, but they should land in a single dedicated PR with the matching seed-grant audit so the swap can be verified end-to-end. |
| **Concurrency safety**                                        | `nextRefNo()` race; no `@@unique` constraint relies on retry. Off-campus cap is correctly inside the txn — good.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **i18n / accessibility**                                      | No i18n library; no a11y review in tests.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **Deployment runbook**                                        | [docs/deployment-runbook.md](./deployment-runbook.md) exists but **content not audited**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **BITS handout structure**                                    | **Schema + renderer + structured editor scaffold shipped (Prompts 11a + 11c + 11d-a).** [handout-schema.ts](../packages/db/src/handout-schema.ts) (`BitsHandoutSchemaV1`) captures the full BITS template; `HandoutVersion.data` (nullable) stores it; [handout-renderer.ts](../packages/db/src/handout-renderer.ts) produces BITS-format HTML; the structured editor at [apps/web/src/app/faculty/assignments/[id]/structured-editor/](../apps/web/src/app/faculty/assignments/%5Bid%5D/structured-editor/) lets faculty author with structured forms. Every read path (SME / faculty / IC / PC / HOG) and the Mode B export ZIP route through [`resolveHandoutHtml`](../packages/db/src/handout-display.ts). The complex repeating sections (rich Part B sub-topics, Evaluation sub-components with live 100% sum validator, full Experiential repeat tables) are MINIMAL in 11d-a; the rich UX lands in 11d-b. Legacy TipTap editor remains accessible for handouts with `data: null` via [`ConvertBanner`](../apps/web/src/app/faculty/assignments/%5Bid%5D/structured-editor/ConvertBanner.tsx).                                                                                                                                                                                         |
| **Handout date formats**                                      | Date fields across BITS handouts use multiple formats (DD-MM-YYYY most common). The schema accepts any string (`partA.date: z.string()`). A normalization helper should be added when the corpus import pipeline lands (Prompt 11f) so all dates persist in a consistent format.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

### Known parser challenges for Prompt 11f (corpus import)

Prompt 11a defined the schema; **Prompt 11f** builds the `.docx → BitsHandoutV1` importer that ingests the real corpus (~385 handouts). The structural-presence sweep that ships with 11a ([handout-schema-corpus.test.ts](../packages/db/src/__tests__/handout-schema-corpus.test.ts)) uses a deliberately crude extractor — unzip `word/document.xml`, strip tags, regex for each required section — and tops out at **~88% section coverage** on the corpus. That ceiling is the _extractor's_ limit, **not the schema's**: every gap traces to one of three docx-extraction problems, and **none requires a schema change.** The 11f parser (mammoth, structure-aware) must handle all three:

1. **Tables, text boxes & SmartArt (extraction outliers) — re-characterized across 11f-a, 11f-b1.** Originally framed as a "table extraction" problem; the 11f-a survey revealed **a different handout TEMPLATE**, not just denser tables, in the `EE ZG5xx/6xx` family ("Course Modules" + per-module Objectives + "Self-Study & Contact Session Plan"). 11f-a treats these as `SKIPPED_MODULE`; 11f-b2 decides interpretive mapping. **11f-b1 surveys settled the remaining 6 "Part A not found" outliers**: `EE_ZG513` was a Module-template file that 11f-a's strict `/^course modules$/i` detector missed (relaxed in 11f-b1 to `/\bcourse modules\b/i` so it's now caught — `SKIPPED_MODULE` count went 8 → 9). The other 5 (`EE ZG613/623`, `POM ZG512/522`, `ST ZG612`) use a **narrative-prose template** — Part A as colon-separated prose lines, text books as un-tabled prose. 11f-b1 added `SKIPPED_NARRATIVE_PROSE` to the enum; parser detects via the `Course No\. : <code>` paragraph signature. Future-work parser path; ≤1.3% of corpus (5/384) so deferring is honest.
2. **Run-split text.** Word fragments a single string across multiple `<w:r>` runs; flattening then inserts spurious whitespace, so `elearn.bits-pilani.ac.in` becomes `elearn .bits- pilani.ac.in` and a contiguous regex misses it. **13** corpus handouts "miss" the eLearn URL purely this way — the URL is present and is _not_ a hidden hyperlink (verified: the URL is absent from `word/_rels/document.xml.rels`). The parser must concatenate adjacent runs before matching.
3. **Section-label synonyms.** The same section is titled differently across departments — the importer needs a synonym map. **Corrected 2026-06-02 from the 11f-a survey:**
   - Experiential Learning ≡ Laboratory **only** (ES ZC424 confirmed). Both label sections that map to `experientialLearning` (lab/practical content, often prose-shape for "Laboratory").
   - **Modular Content is NOT a synonym** — original claim was wrong. DE ZC415's "Modular Content Structure" is a syllabus expansion (a topic outline), not lab/practical. CC ZG506 and SE ZG587 carry BOTH "Experiential Learning" and "Modular Content" at different document positions. 11f-a's parser drops Modular Content content with a `parseWarnings` entry per the data-quality-report discipline below; 11f-b can revisit whether to map it to `partB.sessions[].subTopics` or to extend the schema.
   - "Text Book(s) ≡ Reading Material ≡ Course Material" was an audit-doc claim from earlier prompts that **the 11f-a survey could not confirm**. 0 occurrences of "Reading Material" or "Course Material" across the 12 surveyed files including MBA ZG531 and CC ZG506. May still apply to files outside the survey sample; the parser currently looks only for literal "Text Book". If a real corpus file surfaces this pattern, add the synonym at that point with concrete evidence.
   - **Text-boxes / SmartArt** were also called out as an extraction challenge in this section but the 11f-a survey saw 0 `<w:txbxContent>` and 0-1 `<w:drawing>` across the 12 sampled files including the EE family. The "tables, text boxes & SmartArt" framing should be read as "tables (confirmed); text-boxes (not observed in sampled files; revisit if a counterexample surfaces)".
4. **HHSM-style table-layout variance.** A handful of handouts (notably the `HHSM ZG*` family) lay the Part A header table out so that `Course Title` appears in the cell adjacent to where the regex expects `Course No(s)` — captured value comes back as the title (e.g. `"BIOSTATISTICS & EPIDEMIOLOGY"`) instead of the code. The fix is the same as #1: walk table cells with column awareness, don't pattern-match labels in flattened text. Surfaced by the 11b course-code sweep (`packages/db/src/__tests__/course-codes-corpus.test.ts`).

The sweep is a **proxy** for "would a faithful parser find the required fields", kept only until 11f exists. When the real parser lands, replace the section-presence regex with `BitsHandoutSchemaV1.safeParse(parse(docx))` and raise the threshold (the proxy's ~88% should become ≥95% once tables/runs/synonyms are handled).

> **The 3 MB `.docx` size cap is a deliberate parser-safety threshold, not an arbitrary number (Prompt 11f).** Files exceeding the cap are recorded `SKIPPED_SIZE` (image-heavy `.docx` bloat past it) and are **recoverable** via the `maxBytes` override on `parseDocxToHandout` / `runCorpusImport` — re-import is idempotent, so a higher-cap pass is safe to run anytime. **Raising the cap requires a Phase 1 survey of what the cap was designed to prevent** (parse timeouts / OOM on image-heavy documents); don't bump it blind. The first real corpus run (2026-06-17) skipped **84 of 384 `.docx` (≈22%)** on this cap — a known, documented gap recoverable as a separate operational pass, not a parser failure. (Caveat: the CLI `ImportSummary` tally has no `SKIPPED_NARRATIVE_PROSE` case, so the console summary undercounts by that method's count; `/admin/corpus-imports` is authoritative.)

> **Approved corpus rows are starting points for faculty review, not submit-ready handouts (Prompt 23-a Path B).** Corpus parser extraction is best-effort, not exhaustive — the bulk-approve threshold (`parseWarnings ≤ 1`) gates "good enough to be useful as starting content," NOT "good enough to ship as-is." The editor's completeness gates (e.g. evaluation summing to 100%) enforce data integrity at **submit time**, independent of import quality. Concrete example: the SE ZG501 import extracted only EC-2 (30%) + EC-3 (40%) = **70%** evaluation (the ~30% third component was missed), and carried a `parseWarning` ("Modular Content section ignored"). It was bulk-approve-eligible (1 warning) and pre-populated the faculty editor fine, but the 100%-gate correctly **blocked submit** until faculty added the missing 30%. The system worked as designed: parser extracts what it can → bulk-approve gates usefulness → editor enforces completeness → faculty reviews and completes. **Faculty review + completion remains the canonical quality boundary** — don't "fix" partial extractions by relaxing the editor's completeness gates. (The Modular-Content parseWarning is a known parser limitation recorded as a warning, not a bug; a future Phase 1 could examine enhancing it. Not blocking.)

**11f-a → 11f-b1 honest correction (2026-06-02).** 11f-a reported 286/418 = 68.4% (or 95.3% of attempted-parse files) producing `MAMMOTH_STRUCTURED` data. The metric was measured against "produced Zod-valid `BitsHandoutV1`" but NOT against "extracted meaningful content from all sections." Survey C in 11f-b1 revealed all 286 imports carried placeholder warnings for CO/LO/T-books/Evaluation — the parser was generating placeholder rows for those sections because its table finders, designed against the synthetic-fixture shape, missed the real-corpus shape entirely. Root cause: the synthetic `.docx` fixtures (generated via the `docx` library) used "label-paragraph-then-table" structure; real BITS corpus (via mammoth's HTML output) uses "label-cell-as-first-row-of-table" plus several other variations. The fixture-to-real gap was invisible until the warning-count breakdown was inspected. 11f-b1 fixes 9 specific parser correctness issues (audit §1 has the list) and re-imports the corpus. Post-fix: 92 of 286 imports have zero parseWarnings, 114 have a single warning — **206 of 286 (72%) are now bulk-approve eligible** with real content extracted, not placeholders. The 11f-b1 audit §1 convention "Synthetic test fixtures must be validated structurally against real source files before being trusted" exists to prevent this class of failure recurring.

**11f-b2 final corpus distribution (2026-06-04).** Survey D in 11f-b2 sampled the 47 Evaluation-missing + 103 Part-B-missing files and identified three more parser fixes. The Module template (9 files) got an honest-empty mapping (CO/LO arrays empty per the schema relaxation, populated Part A/B/Evaluation, parseWarnings naming the source gap). Post-11f-b2 corpus re-import (418 files):

| Method                  | Count | % of 418 |
| ----------------------- | ----: | -------: |
| MAMMOTH_STRUCTURED      |   294 |    70.3% |
| SKIPPED_NARRATIVE_PROSE |     5 |     1.2% |
| FAILED                  |     1 |     0.2% |
| SKIPPED_SIZE            |    84 |    20.1% |
| SKIPPED_FORMAT          |    34 |     8.1% |

Of attempted-parse files (excluding pre-flight skips): **294/300 = 98.0% success rate.** Bulk-approve eligible (≤1 parseWarning): **230 of 287 = 78%** (up from 0 in 11f-a and 206 in 11f-b1).

Per-warning evolution across 11f-a → 11f-b1 → 11f-b2:

| Warning                           |   11f-a |  11f-b1 |       11f-b2 |
| --------------------------------- | ------: | ------: | -----------: |
| "Course Objectives missing"       |     286 |      24 |           24 |
| "Learning Outcomes missing"       |     286 |      25 |           25 |
| "Text Books missing"              |     286 |      15 | (consistent) |
| **"Evaluation Scheme missing"**   | **286** |  **47** |        **0** |
| **"Part B sessions missing"**     | **281** | **103** |       **50** |
| "Modular Content section ignored" |      94 |      94 |           94 |
| "HHSM-style swap"                 |      16 |      16 |           16 |

Final FAILED (1): the EE_ZG521 file with the malformed filename `EE ZG521COURSE HANDOUT.docx` (no space between code and "COURSE"). The remaining "Part B missing" 50 files are the AE_ZG614-shape multi-table-per-session-range pattern Survey D classified as accept-with-warning — a future prompt could add a dedicated parser path if scope warrants.

**Corpus-test threshold transition (why 85% is defensible).** The committed threshold in [handout-schema-corpus.test.ts](../packages/db/src/__tests__/handout-schema-corpus.test.ts) is **85%** — calibrated to the proxy parser's structural ceiling (regex over flattened XML), _not_ the schema's correctness. The schema currently fits **100% of corpus content that the proxy can extract**; the 12.3% gap is the parser's, not the schema's. **Prompt 11f's deliverable includes raising the threshold to 95% with the mammoth-based structured parser.** Read six months from now, the 85% is not "we settled" — it is "the test currently measures the proxy, and the schema isn't the bottleneck."

**Data-quality report (11f, separate from schema validation).** Three Part A fields are known to be sometimes-blank in real handouts and are therefore `.optional()` in the schema (`creditUnits`, `versionNo`, `formNumber` — plus the whole `experientialLearning` block). The schema accepts their absence because it is **representational**, not an enforcement layer. **Quality enforcement is a distinct layer the 11f parser must add:** the importer must emit a data-quality report flagging any handout missing `creditUnits`, `versionNo`, or `formNumber`, and the editor (Prompt 11d) should show a non-blocking warning ("This handout is missing Version Number — please add before submitting"). The schema lets faculty save the handout; the UX nudges them to complete it. Two separate layers — don't conflate "the schema permits blank" with "blank is fine".

**Field transformation: `partB.session.subTopics` (join/split contract).** In the source, each session's sub-topics are **one per line** (separate paragraphs/bullets in the Sub-Topics table cell). The schema stores `subTopics` as a single `z.string()`, so the importer joins the lines with `"; "`. This is a **one-way editorial transformation** and the separator choice is load-bearing: a single bullet may itself contain commas (e.g. AEL ZG631 session 3 has the bullet `"OBD, OBD2, OBD3, UDS"` followed by a separate bullet `"Trouble codes"`, transcribed as `"OBD, OBD2, OBD3, UDS; Trouble codes"`). **The renderer (Prompt 11c) must split on `"; "` to recover the bullets — never on `","`** (splitting on commas would shatter `"OBD, OBD2, OBD3, UDS"`). When 11f formalises the importer, `"; "` is the canonical sub-topic separator.

**Experiential-section structure variance (interpretive mapping — owned by 11f).** Even when the experiential text extracts cleanly, the section's internal table layout varies across handouts and does not map 1:1 onto the schema's `experientialLearning` shape (`components` / `experiments` / `labInfrastructure`). The golden fixture ([handout-aelzg631.json](../packages/db/src/__fixtures__/handout-aelzg631.json)) is the canonical live example: AEL ZG631 has **two** experiential tables —

- a "Tut. No. / Practice Tutorial / Description" table → the four hands-on tutorial activities, mapped to **`experiments[]`** (`experimentNumber` = Tut No, `title` = Practice Tutorial, `moduleReference` = Description); and
- a "Sr No / Lab Details / Access" table → lab _modalities and access_ (Virtual Lab via Electude/MATLAB accessed through own system, Remote Lab at the Hyderabad campus, Assignment/Quiz), mapped to **`labInfrastructure[]`**, with `components: []`.

That mapping is a **defensible but interpretive reading**, hand-transcribed for 11a; the canonical, corpus-wide experiential mapping (including whether a handout's "Lab Details" rows are infrastructure vs discrete experiments) is **parser-design work for 11f**. The fixture intentionally preserves this real structure as the example of why 11f needs per-section structure-aware mapping rather than a one-size flatten — it is not a bug, it is a documented placeholder for the downstream parser.

**Course-code corpus sweep (why 80% is defensible).** Prompt 11b added a second corpus sweep ([course-codes-corpus.test.ts](../packages/db/src/__tests__/course-codes-corpus.test.ts)) that asserts ≥1 normalizable BITS course number per file. Measured against the 384-handout corpus: **85.00% pass (255/300 scanned, 84 image-heavy skipped)**, committed threshold **80%**. Same shape and rationale as the 11a section sweep — the threshold is the **proxy parser's ceiling**, not the normalizer's correctness; the normalizer accepts 100% of codes the proxy can extract. The 15% failure breakdown:

- **38 zero-candidate files** — Course No(s) label captured but no BITS code survived run-splitting in the flatten step (e.g. `AIML ZG ZG5 65`, `CC ZG50 4`, `D E ZG5 45`, `HHSM ZG61 7`) or the label itself never landed (EE family, table-heavy).
- **7 all-rejected files** — a candidate emerged, but run-splitting dropped or duplicated the prefix's first character before the normalizer saw it (`DE ZG611` → `E ZG 611`; `ES ZG514` → `SZG514`; `AIML ZC416` → `AIMLC ZC416` with a 5-letter prefix the regex rejects).

37 distinct discipline prefixes were normalized across the corpus (top: `MBA` 63, `SS` 43, `SE` 32, `CC` 19, `ES` 18, `CSI` 16) — the seed's 7 represented prefixes are a small slice of a much wider real catalog. **Prompt 11f's deliverable: raise this threshold to ≥95% once mammoth-based structure-aware extraction handles tables, text-boxes, and adjacent-run joining.**

---

## 6. Three risky areas where future changes need extra care

### Risk 1 — `transition()` and its `effects` callback

[packages/workflow/src/transition.ts](../packages/workflow/src/transition.ts) is load-bearing for the whole app: every workflow change goes through it, side-effects (assignments, approvals, version writes, LMS log) ride inside the `effects(tx, ctx)` callback, and the audit row is part of the same transaction.

Why it's risky:

- Any code change here can silently break atomicity. Example: if a future refactor moves the `auditLog.create` outside the `$transaction`, a failed `effects` would leave an orphan audit row for a transition that never happened.
- The off-campus cap re-check ([hog/requests/[id]/actions.ts:81](../apps/web/src/app/hog/requests/[id]/actions.ts)) lives inside `effects`; that re-check is the only thing preventing two concurrent allocations from both passing the cap. It must run inside the txn and the txn must use a snapshot/serializable isolation level under high load. Prisma default is read-committed, which is **sufficient only because the re-check happens after the status update locks the request row**. A reordering of statements is a subtle data-corruption bug.
- The lazy-creation branch (`if (!handoutId) tx.handout.create({...})`) means the _first_ transition out of DRAFT also creates the `Handout` row. Code in `effects` that assumes `ctx.handoutId` exists is fine, but any future caller bypassing `transition()` and reading `handout` directly will get null.

### Risk 2 — Notification fan-out + SLA dedup

[notifications.ts](../apps/web/src/lib/notifications.ts) is fired on every workflow transition (best-effort) and runs inside the user's request cycle. It's a coupling point between workflow correctness and external systems (SMTP).

Why it's risky:

- It performs N+1 user lookups per event (`usersWithRole` then `usersById`) and N `prisma.notification.findFirst` queries inside `notifySlaReminder` to dedup. Under load this latency becomes user-visible.
- The dedup window is `slaHours/2` and matches on `meta` JSON path. Postgres can index JSON path expressions but the schema has no such index — every reminder sweep does N full-table scans on `Notification`. At ~hundreds of active requests this gets slow.
- The "best-effort" wrapping (`try/catch` everywhere) means notification bugs are silent. A regression that drops 100% of `SUBMITTED` notifications would show up only when PCs notice they're not getting emails — there's no health check.
- `INLINE_FALLBACK` and the DB `NotificationTemplate` table can drift. Editing a template in the admin UI does not invalidate any cache (none exists), but a missing event key in `EVENT_TEMPLATE_KEY` (currently `null` for `EDIT_STARTED` and `ARCHIVED`) means the fallback subject/body wins — easy to miss.

### ~~Risk 3~~ Resolved — `nextRefNo()` race fixed via optimistic retry

**Originally:** [apps/web/src/app/ic/requests/new/actions.ts:18](../apps/web/src/app/ic/requests/new/actions.ts) generated `HMP-YYYY-####` by reading the max existing refNo and incrementing. There was no DB-level uniqueness retry, no advisory lock, no serial counter.

Why it's risky:

- Two simultaneous IC requests in the same year will read the same "last" row, both compute the same next number, and the **second insert will fail with a unique-constraint error** (because `refNo` is `@unique`). The action does not catch that error, so the IC user sees a generic 500.
- If the second insert succeeded for some reason (e.g. someone removes the unique constraint to "fix" duplicates), duplicate refNos break every downstream lookup.
- Fixes: use a Postgres sequence, an advisory lock around the find+create, or a retry loop on unique-constraint violations. Any of those changes must be paired with a test under concurrent load — the current test suite has nothing covering this.

**Resolution:** moved the helper to [ref-no.ts](../apps/web/src/app/ic/requests/new/ref-no.ts) with an optimistic retry-on-P2002 loop (bounded at 5 attempts, jittered backoff). The refNo allocation + audit insert now share a single `prisma.$transaction` in [actions.ts](../apps/web/src/app/ic/requests/new/actions.ts). Detection narrowly matches `err.code === 'P2002' && meta.target` includes `'refNo'` so unrelated constraints propagate. Covered by [ref-no.test.ts](../apps/web/src/app/ic/requests/new/__tests__/ref-no.test.ts) (5 cases including exhaustion and FK-error pass-through) and [concurrent.test.ts](../apps/web/src/app/ic/requests/new/__tests__/concurrent.test.ts) (10 simultaneous real-DB creates → 10 unique sequential refNos, no gaps).

### Risk 4 — `m4-allocate-assign` intermittent first-run failure (unverified hypothesis)

[`apps/web/e2e/m4-allocate-assign.spec.ts`](../apps/web/e2e/m4-allocate-assign.spec.ts) — the IC create → HOG allocate → PC confirm workflow E2E — exhibits intermittent first-run failure in **both** Windows local dev mode AND CI production mode (`next start`). Currently absorbed by the Playwright `retries: 2` config in [`playwright.config.ts`](../apps/web/playwright.config.ts); the test always passes on retry and the job stays green, so the flake is operationally invisible.

Why it's worth naming as an unresolved risk:

- The original `retries: 2` + config-comment diagnosis attributed the flake to **Windows dev-mode RSC streaming hiccups under load** (per-route first-compile, hydration races). That diagnosis was consistent with the evidence at the time. The new data point — the same spec flaking in CI's **production build** (`next start`, pre-compiled bundles, no on-demand compilation) — is inconsistent with a dev-mode-only cause. So either the original diagnosis was incomplete, there's a separate production-mode race we haven't identified, or there's a real-but-rare race in the allocation transition itself that the test surfaces under certain timing conditions.
- Absorbing-via-retry is silencing a signal. The project has been disciplined throughout about not silencing signals; this entry exists so future-us reading the audit knows the flake isn't fully understood.

**Trigger threshold for focused investigation:** if the flake appears in **>1 of every 5 CI runs**, that's the signal it's not random and warrants a focused look at the IC-create / HOG-allocate transition path (suspected race candidates: the optimistic refNo retry loop interacting with the workflow `transition()` effects, or a missing `await revalidatePath` propagation). If the next 2–3 CI runs are clean, the rate is acceptable for now and this stays an open observation.

**When the next failure occurs, capture concrete evidence before declaring environmental.** Specifically: (1) the Playwright `trace.zip` from the failed attempt, (2) which exact assertion timed out (transcribe the locator literally), (3) the proximal step in the IC→HOG→PC sequence (PC-confirm vs HOG-allocate vs IC-create). Without these the "environmental, not regression" claim is unfalsifiable; with them, the next person investigating has a concrete starting point instead of having to reproduce from scratch. Both 11d-b and 11e first-attempt failures had the same proximal step (PC-confirm) — which is the kind of pattern that should accumulate into a hypothesis only if traces back it up.

First observed flaking in CI: PR #13 (Prompt 11b), run [26625535444](https://github.com/syamanth123/wilp-hmp/actions/runs/26625535444). Track future occurrences here as they appear.

**Observed rate as of Prompt 11c:** 1 flake in 4 CI runs — PR #12 (11a) clean, **PR #13 (11b) flaked + retry-passed**, PR #14 (docs) clean, PR #15 (11c, second run after the sanitizer-swap fix) clean. 25%; at the threshold edge but trending clean (last two runs). If the next 1–2 CI runs stay clean, the rate falls below the >1/5 threshold and this stays a passive observation. A fresh flake brings the rate to 2/6 = 33% and warrants investigation.

**Observed rate as of Prompt 11d-a (CI gap):** PR #16 could not be CI-validated — see "CI gap (post-PR-#15)" below. No new m4 data point. Rate stays 1/5 = 20% until the account hold is resolved and CI runs again.

### Risk 5 — `m10-structured-editor` first-run flake (one observation)

[`apps/web/e2e/m10-structured-editor.spec.ts`](../apps/web/e2e/m10-structured-editor.spec.ts) — extended in Prompt 11d-b (PR #17) to a comprehensive flow exercising chip-list add/remove, references typeahead from Part A T/R codes, multi-EC evaluation with the 100% UI-only rule disabling/enabling Save, experiential nested repeats, the structured AI dialog stub path, save → preview parity, and reload-persistence. During 11d-b's local verification under `--repeat-each=5`, the first attempt was **4/5** and a clean retry of the same command produced **5/5** — cumulative **9/10 = 90%**.

Classed alongside Risk 4 as **environmental, not a regression**: the flaking iteration is non-deterministic across attempts; no failure has been reproduced when re-running the spec immediately afterward.

**Observed pattern across two PRs.** PR 11d-b: first attempt 4/5; second attempt 5/5. PR 11e: same shape — first attempt 4/5 with the failed iteration timing out at `page.getByRole('button', { name: /confirm assignment/i }).click()` (the PC-confirm step, which is the HOG→PC handoff that constitutes the m4 Risk 4 surface); second attempt 5/5. The m10 spec runs the full IC→HOG→PC→Faculty setup before reaching the editor assertions, so a failure in that prelude registers against m10 as well as against the (already-known-flaky) HOG→PC transition. **The two observations strongly suggest Risk 5 is Risk 4 re-surfacing through m10's longer prelude, not a distinct editor-side flake.** If a future failure lands in the editor body (e.g. after `bits-structured-editor` is visible — chip-list assertions, evaluation transition, AI dialog), the original watch-list below applies:

- AI dialog mount timing: the spec opens the dialog and immediately asserts on the stub banner. The dialog's `useEffect`-triggered auto-run on `open` may race with the assertion under load.
- `onValidityChange` callback firing on initial render: `EvaluationScheme` notifies the root editor of the initial `evalValid` value via a `useEffect` after first paint; if the spec asserts the Save button's disabled state before that fires, it could see a stale rendered state.
- Production-build hydration: the spec runs against `next start`. If chunks for the structured editor's lazy-loaded TipTap or chip components arrive out of order, the first interaction can race.

Tracked in [docs/flake-tracker.md](./flake-tracker.md) alongside m4. Trigger threshold the same as Risk 4: investigate if the rate climbs above **>1 of every 5 runs**. The merged Risk 4/5 first-attempt rate as of 11e is **2/10 = 20%** — at the threshold edge, monitoring continues, no investigation triggered yet.

### Risk 6 — Verification gap: orphaned E2E specs went unnoticed (discovered in 12-a, retroactively affecting PRs #18–#21)

The local "existing E2E stays green" check across the template-stack PRs implicitly trusted that the **full** E2E suite was being run. It wasn't, consistently. `m5-faculty-edit-submit` went red in **PR #18 (11e)** — once `startEditingAction` began producing a structured `HandoutVersion`, "start editing" rendered the `StructuredEditor` instead of the legacy TipTap editor, and m5's selectors (`.ProseMirror` typed-into, then a "Save version" click that now resolves to the StructuredEditor's `bits-save-button`, which the 11d-b eval-100% rule disables on an empty-eval template) became obsolete. The spec was effectively orphaned by a default-path change. It stayed red **unnoticed through PRs #18, #19, #20, #21** because each PR's verification focused on m10 (the structured spec) and didn't re-run / notice m5. The account-level CI hold (see below) means no automated regression surfaced it either.

**Discipline correction.** A PR touching a workflow path must **explicitly enumerate which E2E specs exercise that path and confirm each one's state — pass, OR documented "red since PR #N" with the attribution.** Don't report "all existing specs green" without naming what "all" is; "green" against a subset that silently shrank as default paths changed is how m5 hid for four PRs. When CI returns, a full-suite run on the first green CI will re-baseline every spec; until then, the enumerate-and-confirm step is the manual substitute. (12-a found this by investigating an m5 failure rather than patching the spec; 12-b decided m5's fate — deleted, as orphaned-and-unrepairable-as-is.)

**Lessons reinforced in 12-b (live-run findings).** The 12-b live E2E run surfaced three patterns worth keeping:

- **Behavioral validation changes break un-enumerated callers at runtime, not compile time.** When changing a server action's validation rules (12-b made `allocateFacultyAction`'s `smeUserId` required), enumerate every fixture / helper / spec that calls it. A _signature_ change compile-fails its callers; a _behavioral_ change (requiring a previously-optional field) does not — `m6c-comments` and `m8-ai-layer` compiled clean but failed at runtime on a disabled Allocate button, because they allocate via the shared UI flow and weren't updated alongside m4/m10. Compile-clean + unit-green cannot catch this; only a full live run can.
- **E2E specs must assert post-revalidate PERSISTENT state, not transient client success messages.** A server action's `revalidatePath` re-renders the server component before Playwright can observe the client-side success banner, so asserting the banner fails (it's swapped out for the re-rendered state). Target the persistent post-revalidate UI — a status badge, a read-only note — instead. (`m4-sme-approval-loop` hit this; same root as the m5-era lesson.)
- **SMTP / Mailhog / Redis / MinIO verification gaps are the standard probe-skip pattern, not unknowns.** Specs gate on a reachability probe and skip when the service is down (no Docker locally). Those paths get verified the moment Docker is available (Docker Desktop, a self-hosted runner, or restored GitHub Actions). Until then the in-portal `Notification`-row assertion is the strongest available evidence for notification routing (see Risk 7).

### CI gap (post-PR-#15)

Starting **2026-06-01**, GitHub Actions runs on this repo's branches began failing at the runner-allocation step with `startup_failure` (no logs; runner never came up). Root cause: **account-level Actions hold** stemming from an unpaid balance accrued during prior private-repo CI usage. Standard mitigations attempted:

- Repo flipped from private → public to access the unlimited free-tier Actions minutes for public repos → **didn't help**; the hold is account-level, not repo-level. The failure mode changed (`startup_failure` → `failure` with `steps: []` and ~4s runtime) but no actual workflow steps execute.
- GitHub Student Developer Pack — held by the user but **does not** lift an existing account-level overage hold (Pack credits apply to future usage, not back-charges).
- `gh run rerun` — not permitted for `startup_failure`-class runs.

**Consequence for PR #16 (Prompt 11d-a):** merged onto `main` as `30951db` **based on local evidence only**: typecheck + lint clean across `@hmp/db` / `@hmp/web` / `@hmp/integrations`; **96 `@hmp/db` unit tests + 79 `@hmp/web` unit tests passed**; **m10 E2E spec passed 5/5 under `--repeat-each=5`** on Windows dev-mode (production-build CI would typically be cleaner). This is a documented deviation from the project's "CI green before merge" rhythm, explicitly ratified by the user given the external nature of the block.

**Consequence for the m4 flake tracker** (`docs/flake-tracker.md`): the running rate calculation stops accumulating new data points until CI runs again. The 1/5 = 20% figure freezes at PR #15. When the account hold is resolved, the next CI run resumes the tracker.

**Resolution paths** (any of these unblocks CI):

- Pay the outstanding balance to clear the hold.
- Switch development to a different GitHub account with its own free-tier quota (creates a history-fork; old account retains PRs #1-#16 as historical record).
- Self-hosted runner on a maintainer machine — may work for the `ci` job (no service containers needed beyond what runs locally) but not for the `e2e` job (needs Docker for MinIO/Mailhog/Redis services). May or may not be blocked by the hold (typically self-hosted runners bypass the billing block; not yet tested).

Future PRs should resume the standard rhythm (branch → PR → CI green → merge) the moment CI is unblocked. Until then, any merge based on local evidence should add a row here documenting the gap, naming the local verification artifacts (typecheck / lint / test counts / e2e results).

### Risk 7 — Notification-firing gap: actions that transitioned state but never notified (discovered in 12-b, retroactively affecting 12-a's SME actions)

`smeApproveAction` and `smeRevertAction` (introduced in 12-a) wrote the state transition and the `Approval` row but did **not** call `notifyTransition`. The unit-test layer (which checks state + DB writes) couldn't catch this; only the E2E layer (which checks the notification side effect) could. Across 12-a's shipping and 12-b's first 16 steps, **no notifications fired for SME approve/revert** despite the templates being defined (`SME_APPROVED`/`SME_REVERTED` in notification-templates.ts) and the recipient logic being in place (`computeRecipients`). It surfaced only when the `m4-sme-approval-loop` E2E expected the notification and it wasn't there.

This is the same shape as Risk 6: a higher integration layer catches what the unit layer passes. Same root cause — trust in incomplete verification.

**Fixed in 12-b:** both actions now call `notifyTransition` (best-effort, matching the HOG/PC actions). `m4-sme-approval-loop` asserts the side effect directly — `SME_REVIEW_REQUESTED` → SME + IC, `SME_REVERTED` → faculty + IC, `SME_APPROVED` → faculty + IC + PC — by querying the in-portal `Notification` rows (which write inline when Redis is down, so they're assertable without a worker).

**Discipline correction:** a new server action that performs a state transition MUST include its `notifyTransition` call — treat it as a pre-commit checklist item. Verification-gate upgrade: integration/E2E tests for new actions must assert **notification side effects**, not just state + DB writes. (The `Notification`-row assertion pattern in `m4-sme-approval-loop` is the template to copy.)

### Deferred work (Prompt 17 — AI cost tracking)

> **Deferred: budget alert email delivery.** The current budget warning writes in-portal `Notification` rows directly from `@hmp/ai` using shared prisma ([usage.ts](../packages/ai/src/usage.ts) `maybeWarnBudget`). Email delivery requires the `apps/web/src/lib/notifications.ts` SMTP `deliver()` path, which `@hmp/ai` can't import (circular dependency — apps/web depends on @hmp/ai, not vice-versa). Resolution options when this becomes important: (a) extract notification dispatch into a shared package both apps/web and @hmp/ai can import; (b) emit a budget-warning event that apps/web's notification worker subscribes to and dispatches via BullMQ; (c) duplicate the SMTP logic in @hmp/ai (worst option). **Option (b) is cleanest — it fits the existing BullMQ pattern.** Acceptable to defer: budget alerts are infrequent (once/month via dedup) and the admin sees the in-portal alert during routine work.

> **Deferred: hard budget cap.** Prompt 17 ships a SOFT cap only (track + warn; AI stays enabled over budget). A hard cap (block AI calls once the monthly budget is exhausted) needs: a pre-flight month-to-date check before every AI call (a new chokepoint cost, vs the current post-call recording); graceful UI degradation (the AI feature must return an "AI paused — monthly budget reached" message, not a 500); and an admin override (a per-month or per-request bypass so urgent work isn't blocked). Defer until real spend data shows it's needed — the soft cap's warning gives the signal to decide.

---

## Appendix — packages map

| Package             | Purpose                                                                                                                  |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| `@hmp/db`           | Prisma client + schema (single source of truth).                                                                         |
| `@hmp/auth`         | NextAuth config, RBAC helpers (`hasRole`, `requireRole`, `requirePermission`, `loadUserAccess`), SSO provider interface. |
| `@hmp/workflow`     | State machine, transition table, `transition()` orchestrator, role + cap guards.                                         |
| `@hmp/ai`           | Provider abstraction, embeddings, recommender, quality report, handout draft (with stub fallback).                       |
| `@hmp/integrations` | CSV parsing + ERP schemas, Taxila LMS stub, Nodemailer SMTP.                                                             |
| `@hmp/ui`           | Tailwind shadcn-style primitives.                                                                                        |

CI ([.github/workflows/ci.yml](../.github/workflows/ci.yml)): pnpm install → prisma generate → prisma db push → lint → typecheck → test → build. Postgres service container. `AI_PROVIDER=noop` so tests hit stubs. No e2e run in CI — Playwright specs exist but are not invoked.

---

## 7. Resolved unclear findings

### A) AI metrics aggregation — **mostly complete; one real gap**

Original §3 entry called this "TBD". Reality: the page does real aggregation queries.

[apps/web/src/app/admin/ai-metrics/page.tsx:39-62](../apps/web/src/app/admin/ai-metrics/page.tsx) runs six queries in `Promise.all`:

- `prisma.aIRecommendation.findMany` over last 14 days (createdAt, model).
- `prisma.aIQualityReport.findMany` over last 14 days (createdAt, model, score).
- `prisma.aIDraftLog.findMany` over last 14 days (createdAt, model, source) plus `aIDraftLog.count()` for lifetime.
- `prisma.embedding.groupBy` by `(ownerType, model)`.
- `prisma.auditLog.findFirst` where `action: { startsWith: 'ai.' }` for "last AI audit entry".

It renders three cards: **Provider status** (active provider, OPENAI/ANTHROPIC key presence, last AI audit entry); **Usage (last 14 days)** with a per-day table of recs/reports/drafts/models plus avg quality score; **Embedding corpus** grouped by `ownerType` with the re-embed action. The "re-embed all" button wires to `runReEmbedAction` ([actions.ts:8](../apps/web/src/app/admin/ai-metrics/actions.ts)) which calls `ensureCorpusEmbeddings()` — real, not a placeholder.

What's actually missing — and stated in-line at [page.tsx:208-211](../apps/web/src/app/admin/ai-metrics/page.tsx):

> _"Per-call token counts are not persisted yet — cost rollup will be added once the AI provider returns and we capture token usage per request."_

This matches §5's separate "AI cost controls" gap: token counts live inside `AIDraftLog.payload` JSON (and aren't persisted at all for `AIRecommendation` / `AIQualityReport`). Without schema changes to promote `promptTokens` / `completionTokens` to columns, no cost dashboard is possible. The page is feature-complete for everything it can query today; the gap is in the data model, not the UI.

**Verdict:** §3 entry overstated this gap. Correction: page is implemented; cost rollup is blocked on schema work covered by §5.

### B) File upload presence — **no upload path exists anywhere**

Definitive answer: nothing in the codebase uploads files.

Searches and results:

- `presigned|S3Client|@aws-sdk|getSignedUrl|PutObjectCommand` across `{apps,packages,infra,docs}/**/*` — **no matches in source.** Only hits are the `S3_*` env-var comments in [.env files](../apps/web/.env), the `MINIO_*` config in [infra/docker-compose.yml:35-48](../infra/docker-compose.yml), and the `s3Key` column at [schema.prisma:323](../packages/db/prisma/schema.prisma). No SDK code.
- `aws-sdk|minio|@aws` across all project `package.json` files — **no matches.** The only hit is inside `node_modules/.pnpm/nodemailer@6.10.1/.../package.json` (a nodemailer keyword). Zero S3 SDK in any project package.
- `Attachment.create|attachment.create|prisma.attachment` across `{apps,packages}/**/*.{ts,tsx}` — **no matches.** The `Attachment` model is never written.
- `attachment|Attachment` across the same scope — three hits only: the model definition ([schema.prisma:201,317](../packages/db/prisma/schema.prisma)) and one read at [ic/requests/[id]/page.tsx:25](../apps/web/src/app/ic/requests/[id]/page.tsx) (`attachments: true` in the include). That read is **dead** — the JSX in lines 80-180 of the same file never references `request.attachments`.
- `<input type="file">`-style HTML / `multipart` / `formidable` / `busboy` — **no matches** in `apps/`.
- The "CSV importer" at [admin/import/csv-importer.tsx:97](../apps/web/src/app/admin/import/csv-importer.tsx) is a `<textarea>` — users paste CSV text. There is no file picker even there.
- `previousHandoutUrl` in [HandoutRequest](../packages/db/prisma/schema.prisma) is a plain string URL — users paste a link, no upload.

**Verdict:** §5 entry was correct that no upload path exists. Strengthening it: there is no S3 SDK installed, no upload route, no client-side file input, no producer of `Attachment` rows. The `Attachment` model and `s3Key` column are pure schema-only; MinIO in compose is unused by the app. Building uploads is a from-scratch task, not a "wire up existing".

### C) Soft-delete consistency — **misdiagnosed; nothing reads `deletedAt`**

Original §5 entry assumed there were inconsistent filters on `User.deletedAt`. Wrong premise.

Search: `deletedAt` across `{apps,packages}/**/*.{ts,tsx,prisma}` — **exactly one match:** [schema.prisma:44](../packages/db/prisma/schema.prisma), the column definition itself. **No code reads it, no code writes it.** It is dead schema.

The actual deactivation mechanism is the `User.active: Boolean` column. That field IS used consistently in every query where it matters:

| Site                                                                                                                   | Filters on `active`?                          | Correct?                                                 |
| ---------------------------------------------------------------------------------------------------------------------- | --------------------------------------------- | -------------------------------------------------------- | ------------ | --- | -------------------------------- | ----------------------------------- |
| [packages/auth/src/config.ts:27-28](../packages/auth/src/config.ts) — login                                            | No `where`, but programmatic guard `if (!user |                                                          | !user.active |     | !user.passwordHash) return null` | Yes — inactive users cannot log in. |
| [packages/ai/src/recommender.ts:85,99](../packages/ai/src/recommender.ts) — faculty pool                               | `active: true`                                | Yes                                                      |
| [packages/ai/src/embeddings.ts:67,70,122](../packages/ai/src/embeddings.ts) — embedding corpus                         | `active: true`                                | Yes                                                      |
| [apps/web/src/lib/notifications.ts:97,106,354](../apps/web/src/lib/notifications.ts) — recipient lookups               | `active: true`                                | Yes                                                      |
| [apps/web/src/lib/faculty-load.ts:33](../apps/web/src/lib/faculty-load.ts) — load calc                                 | `active: true`                                | Yes                                                      |
| [apps/web/src/app/hog/requests/[id]/actions.ts:65](../apps/web/src/app/hog/requests/[id]/actions.ts) — allocate picker | `active: true`                                | Yes                                                      |
| [apps/web/src/app/admin/users/page.tsx:9](../apps/web/src/app/admin/users/page.tsx) — admin user list                  | No filter                                     | Correct — admins must see inactive users to toggle them. |
| [apps/web/src/app/admin/users/actions.ts:31,58](../apps/web/src/app/admin/users/actions.ts) — admin CRUD               | No filter                                     | Correct — admin operates on all users.                   |

**Verdict:** there is no inconsistency. `active` is enforced everywhere it should be; admin paths intentionally bypass it. The §5 finding was wrong. The real issue was that **`User.deletedAt` was dead schema** — **RESOLVED in Prompt 18, which dropped the column** (rather than wire up a redundant second soft-delete signal) and canonicalized the `active` filter. It also found + fixed the one genuine leak the broader concern pointed at: `bulk-allocate` wasn't filtering `active`. See §1 "User soft-delete (Prompt 18)."

### D) TODO / FIXME quick scan — **clean**

Search: `TODO|FIXME|XXX|HACK` across `{apps,packages,infra,docs,scripts}/**/*.{ts,tsx,mjs,prisma,yml,yaml,md,json}`.

**One match, and it is the sentence in §3 of this audit document claiming there are none.** Zero markers in actual source. The stubs documented in §3 are the only mocked paths.

---

## 8. Confirmed action items

From A–D above. "PROMPT N" labels are placeholders for the next round of work; "NEW" means a follow-up that wasn't in the audit's first pass.

| #   | Action                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Source finding         | Label                                                                                                                                                                                                                                                                          |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | Promote `promptTokens` / `completionTokens` from `AIDraftLog.payload` JSON to indexed columns; persist token counts on `AIRecommendation` and `AIQualityReport` too. Then add a cost-rollup card to the AI-metrics page.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | §7-A                   | **DONE (Prompt 17).** Took the `AiUsageLog` cost-ledger route (one row per real call) instead of columns on the output models — better for a financial record (`onDelete: SetNull`, orthogonal to disposable artifacts). Cost-rollup cards added to ai-metrics.                |
| 2   | Capture token usage from provider responses in [packages/ai/src/openai.ts](../packages/ai/src/openai.ts) and [packages/ai/src/anthropic.ts](../packages/ai/src/anthropic.ts); thread into the AIDraftLog/AIQualityReport/AIRecommendation writes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | §7-A                   | **DONE (Prompt 17).** `chatJson` already returned tokens; `embedText` now surfaces them too (openai.ts). Threaded into `recordAiUsage` at the chokepoint, not the output-model writes.                                                                                         |
| 3   | Update §3 of this audit to remove `ai-metrics aggregation` from the "unclear stubs" list; queries are real.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | §7-A                   | **Done in this PR** (§3 row replaced with the AI token-cost columns gap).                                                                                                                                                                                                      |
| 4   | Decide: build file upload (MinIO/S3 client + presigned PUT route + `<input type="file">` UI + `prisma.attachment.create`) OR drop `Attachment` model + `s3Key` column + the dead `attachments: true` include at [ic/requests/[id]/page.tsx:25](../apps/web/src/app/ic/requests/[id]/page.tsx).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | §7-B                   | **NEW** — feature-sized decision.                                                                                                                                                                                                                                              |
| 5   | If building uploads (#4), add an `@aws-sdk/client-s3` or `minio` dep to `packages/integrations`, wire `S3_*` env vars, add an upload route handler under `apps/web/src/app/api/uploads/`, and replace the textarea-paste CSV importer with a file picker.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | §7-B                   | **NEW** (conditional on #4).                                                                                                                                                                                                                                                   |
| 6   | Drop the **"Tenancy / soft-delete inconsistency"** row from §5 — it's a false alarm. Replace with "**Dead column `User.deletedAt`** — never read or written. Decide: implement hard-erase tracking, or drop the column in a migration."                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | §7-C                   | **Done.** §5 row replaced (audit PR). **Schema decision RESOLVED in Prompt 18: column dropped** + `active` filter canonicalized + bulk-allocate `active` gap fixed.                                                                                                            |
| 7   | Update §3's `Nothing in the audited source files contains TODO/FIXME/XXX/HACK markers` to clarify "across the full repo, not just audited files" — verified.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | §7-D                   | **Done in this PR** (sentence rewritten to "entire repo … verified via repo-wide `rg`").                                                                                                                                                                                       |
| 8   | **Prompt 8 wording-regression guardrail** — when Prompt 8 seeds the SME notification templates (`handout.sme_nominated`, `handout.sme_accepted`, `handout.sme_declined`, `handout.sme_completed`), each seeded template's `subject` and `body` MUST equal or improve on the inline fallback wording in [apps/web/src/lib/notifications.ts](../apps/web/src/lib/notifications.ts) (the four `notifySme*` functions). The fallbacks are what real recipients see in production between Prompt 6/7 landing and Prompt 8 landing, so the templates must not introduce a copy regression. Diff each pair before merging Prompt 8.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Prompt 6/7 → 8 handoff | **Done in Prompt 8** — each seeded template verified equal-or-better than its fallback; fallbacks retained as the safety net. A regression of a _different_ kind (template referencing a token the supplier doesn't pass) is now blocked by the token-contract test (item 12). |
| 9   | **SME nomination uniqueness — DB-level guard** — Prompt 6 enforces the "no duplicate active nomination" rule in app code inside a `$transaction`. The stronger fix is a partial unique index on `(requestId, smeUserId) WHERE status IN ('PENDING','ACCEPTED')`, which makes the constraint race-free at the DB level. Requires a migration; defer until concurrency demands it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Prompt 6 follow-up     | **NEW** (small migration, low priority).                                                                                                                                                                                                                                       |
| 10  | **`Comment(handoutId, authorId)` composite index** — Prompt 7's `completeNomination` helper queries `comment.findFirst({ where: { handoutId, authorId } })` to enforce the mark-complete prerequisite. The existing `@@index([handoutId])` makes this efficient at current volumes (handouts have ≤ ~30 comments), but a composite `@@index([handoutId, authorId])` would eliminate the post-index filter. Defer until a hot handout is observed in metrics.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Prompt 7 follow-up     | **NEW** (one-line schema change, no urgency).                                                                                                                                                                                                                                  |
| 11  | **Prompt 7 progress** — SME route group landed: dashboard at `/sme`, list at `/sme/nominations`, detail at `/sme/nominations/[id]` with accept / decline-with-reason / mark-complete. Pure helpers extracted to [sme-response.ts](../apps/web/src/app/sme/nominations/%5Bid%5D/sme-response.ts) for unit-testability. SME comments enabled via [`comment-actions.ts`](../apps/web/src/app/%28shared%29/comment-actions.ts) — ACCEPTED-only nominations grant write rights. Faculty page now shows a read-only "SMEs advising" panel above the comment thread. After this lands, the SME feature is functionally complete end-to-end; only Prompt 8 (notification template seeds + multi-role PC↔SME E2E) remains.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Prompt 7               | **Done (PR #6).**                                                                                                                                                                                                                                                              |
| 12  | **Prompt 8 — SME notification templates + multi-role E2E (feature complete).** Four `NotificationTemplate` rows seeded (`handout.sme_nominated` / `_accepted` / `_declined` / `_completed`), defined in a shared [`SME_NOTIFICATION_TEMPLATES`](../packages/db/src/notification-templates.ts) constant imported by both `seed.ts` and the token-contract unit test (zero drift). The four `notifySme*` inline fallbacks remain as the safety net; templates are now the active path. **Token-contract guard** ([notifications.test.ts](../apps/web/src/lib/notifications.test.ts)) renders every seeded SME template against its real token supplier and asserts no residual `{{token}}` survives — catches the class of bug where a template references a token the supplier never provides (table-driven; one row per future template). Multi-role E2E [m4d-sme-multi-role-loop.spec.ts](../apps/web/e2e/m4d-sme-multi-role-loop.spec.ts) walks PC→SME→PC+faculty with real Mailhog email assertions (gated on a Mailhog probe; CI provisions a Mailhog service + readiness wait). With this the **SME advisory feature is complete end-to-end** (schema → RBAC → PC inbound → SME outbound → templates + multi-role E2E) — the first deployment-grade feature shipped through the PR rhythm. | Prompt 8               | **Done in this PR.**                                                                                                                                                                                                                                                           |

Items 3, 6, and 7 are corrections to this very document and were folded in during the docs-honesty pass that landed alongside this audit (see `docs: honest README + RFP traceability + audit corrections`). Items 1, 2, 4, 5 are real code work; 4 is the largest (a from-scratch upload feature). Items 8 and 9 are forward-looking guardrails added during Prompt 6.
